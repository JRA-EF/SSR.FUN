# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env.local
.env.vercel.*

# Solana / Anchor -- never commit keypairs or local validator state
*-keypair.json
target/
.anchor/
test-ledger/
devnet-fixtures/

# Scratch lockfiles Vercel's local `vercel build`/`vercel --prod` generates
# per-function inside api/** during its own zero-config dependency install --
# not meaningful, never intentionally created.
api/**/package-lock.json
