<!--
  Source of truth for the internal project-status dashboard at /internal/status.
  This file is parsed server-side (api/dashboard/content.ts) and rendered after
  authentication. Keep section headings ("## ...") stable -- the parser splits
  on them. The fenced ```json block under "Roadmap" is the only piece treated
  as structured data (weight + completion per phase); the overall completion
  percentage is computed from it, not hand-entered.

  Update this file whenever meaningful progress, roadmap status, blockers,
  dependencies, risks, validation results, or environment status change. See
  CLAUDE.md > "Project Status and Decision Logging".
-->

## Overall Status
On Track -- SSR Protocol is live on Solana DevNet, corrective pass and treasury upgrade both deployed to production.

## Current Phase
**The full 8-phase A-H DevNet delivery plan is complete** (see `docs/project/DEVNET_IMPLEMENTATION_PLAN_2026-07-29.md`): Phases A-D and F-G are implemented, deployed, and live-DevNet-verified (DEC-0041 through DEC-0044, DEC-0046, DEC-0048, DEC-0049, DEC-0050); Phase E (Jupiter DevNet swap feasibility) was proven infeasible and the resulting product decision was made (DEC-0045, resolved by DEC-0051). **A real on-chain DevNet swap mechanism (`ssr_devnet_amm`) was subsequently started to act on DEC-0051 but is incomplete and has been deliberately deferred**: it is preserved, isolated, on a separate `experimental/devnet-amm-swap` branch (commit `d6d1979`), NOT merged into `main`, NOT wired into any live page, and NOT active (deployed to DevNet but never initialized -- no config, no pools, no liquidity exist for it on-chain). Direct proportional redemption remains the canonical, only Sell path in production. On-chain delegate signing for manager actions (beyond root-manager-only) also remains a documented follow-up, not started.

## Current Objective
All approved Phase A-H work is complete, deployed, and live-verified in production. Three items remain, none of them blockers: (1) finish and properly land the `ssr_devnet_amm` swap mechanism from its experimental branch, if/when resumed; (2) wire on-chain delegate signing for manager actions generally; (3) perform a real browser + Phantom click-through (no browser-automation tool available in this environment -- everything below the UI layer is verified against live DevNet, but the literal click path has never been human/browser-tested).

## Executive Summary
**SSR Protocol went from "not started" to "deployed live on Solana DevNet, fully tested against that deployment, and wired into the real frontend with working Buy/Sell and Create Reserve flows" across the initial build session, then underwent a focused corrective pass (this entry) driven by manual DevNet testing.** Architecture was locked, the 17-instruction Anchor program and a TypeScript SDK were hand-written, a complete native toolchain was installed piece by piece, and the program was deployed and verified live at `2dURvmSdHeyaFES5rxaE1zgPSHCBLW5BLNguJ2Tu1mkW`. Its full test suite passes against that live deployment (14/14), and persistent Gate-9 fixture Reserves exist on-chain (`docs/protocol/DEVNET_FIXTURES.md`). Gate 10-11 then wired the existing SSR.fun frontend to all of this while preserving its design exactly (DEC-0027).

**This corrective pass (2026-07-28) fixed a real bug that blocked every newly created Reserve from Buy/Sell**: `api/devnet/swap-sign.ts` hardcoded an allowlist of just the 2 Gate-9 fixture Reserve addresses, rejecting any genuinely new Reserve outright. Replaced with fully dynamic, on-chain-derived validation that trusts no hardcoded address -- only a per-asset-mint allowlist (DEC-0029). Also added: native SOL/wrapped-SOL as a real Create-Reserve asset (DEC-0030); combined create+register into one transaction, cutting Create Reserve to 2 wallet approvals (3 with a SOL leg), plus a real rent-calculator-backed "Wallet Cost Summary" shown before any signature (DEC-0031); an `update_protocol_config` instruction to repoint the DevNet protocol treasury (DEC-0033); subtle DevNet-transaction status messaging (clickable Explorer links, a per-trade "real DevNet transaction" note). Validated end-to-end against a **genuinely fresh Reserve created through the real client code itself** (not just the existing fixtures), catching and fixing a second real bug in the process -- wrapped-SOL seed funding wasn't converting USD through the DevNet SOL test price, asking for ~20x too much SOL (DEC-0032). `npm run build`, `npx tsc -b`, and `oxlint` all pass clean. This pass is committed, pushed, and deployed to production. **The DevNet protocol-treasury upgrade (DEC-0033) is also now deployed and verified** -- the user funded the deployer wallet directly, the `update_protocol_config` upgrade landed on-chain, and a real `collect_fees` call confirmed the treasury actually receives fees (DEC-0035). Remaining: an actual browser/Phantom click-through (no browser-automation tool was available in this environment for the frontend flows) and enabling Windows Developer Mode so localnet testing (DEC-0034) is fully usable.

## Last 5 Working Days
- **2026-07-30 (Thu), Helius RPC integration + Buy 429/"insufficient SOL" diagnosis pass (later the same day):** Adopted Helius as the primary DevNet RPC provider everywhere (DEC-0055): every `api/devnet/*.ts` handler now resolves its Connection through a new shared `resolveRpcUrl()` (Helius when `HELIUS_RPC_URL` is set, falling back to the prior public-endpoint default) instead of each duplicating its own literal default; the browser's Connection is now pointed, in production/preview builds only, at a new narrow server-side proxy (`api/devnet/rpc-proxy.ts`) instead of ever receiving a raw provider URL/API key -- the proxy allowlists only the 9 JSON-RPC methods this app's own Connection genuinely calls (confirmed by an exhaustive grep, including Anchor/spl-token's internal `getAccountInfo` dependency), validates/bounds every payload, applies a best-effort per-IP throttle, and falls back once to the public endpoint on a genuine transport failure -- never on a 429, and never for `sendTransaction` (a send is never retried against a second endpoint; reconciliation is always by signature). `HELIUS_RPC_URL` was added directly to the linked Vercel project via `vercel env add` (Production/Preview/Development), never committed to any tracked file. **Diagnosed the reported 50-devUSDC Buy failures** by tracing the exact unsigned transaction end-to-end: devUSDC's mint/decimals (6, correctly used throughout -- no double-scaling, no lamports confusion), every instruction, the fee payer (always the connected wallet, confirmed), and every lamport transfer. Found two real, confirmed bugs, both unrelated to decimals: (1) `swap-sign.ts`'s `fetchReserveOnChain` call sat OUTSIDE its own try/catch, so a 429 there (confirmed live: the public endpoint returned the exact raw `"429 Connection rate limits exceeded"` string this session while investigating) escaped as an unhandled exception instead of a formatted error -- fixed by moving it inside the try block and wrapping every RPC read in this endpoint (previously un-retried, unlike the frontend's own reads) in the existing `withRateLimitRetry` helper (DEC-0056); (2) Buy's wrapped-SOL leg (when a Reserve has one) and 100% of Sell's payout are funded from the DevNet swap authority's OWN SOL balance, never the user's -- with no sufficiency check, a low swap-authority balance produced a transaction Phantom's own preflight simulation would reject for a signer OTHER than the connected wallet, which Phantom's generic UI reports as "insufficient SOL" with no way to tell it apart from the user's own balance. A new `assertSwapAuthorityHasSol` check now fails closed with an honest, correctly-attributed error before ever returning such a transaction (DEC-0056). Also migrated the two remaining `connection.confirmTransaction` (websocket-based) call sites (`managementClient.ts`, `createReserveClient.ts`) to the already-proven bounded `confirmSignatureBounded` (DEC-0057) -- required since the HTTP-only proxy has no persistent websocket to subscribe through. Client-side: `zapClient.ts`'s error now carries a structured `code` (`rpc_congested`/`swap_authority_low_sol`/`build_failed`); `DTRDetail.tsx`'s Buy/Sell show distinct, accurate messages per code and pre-check the trader's own real devUSDC/Reserve-Token balance before any network call. 17 new offline tests (`tests/phase_helius_rpc_and_buy_fix.ts`; 100/100 total passing). `tsc -b`, `vite build`, `oxlint`, `tsc -p api/devnet/tsconfig.json`, `tsc -p scripts/tsconfig.json` all clean. **Live-verified**: a new `scripts/verify_helius_buy_fix.ts` ran the exact `executeBuyZapDevUsdc` function DTRDetail.tsx calls, genuinely through the Helius endpoint, buying 50 devUSDC (the exact reported amount) against the Phase C Reserve -- signature `hGDWXhLtLFHrsJssUd7YzQxXcyY4oQJdqVjTxLVMoNsrYCqHMdxJK1zcDNHvJTvunYkJr13j5Acr1fQacTLj1Sa`, trader SOL 0.050000 -> 0.045911 (only 0.004089 SOL spent on ATA rent + network fee -- confirms the connected wallet's own SOL need is tiny and was never the real constraint), devUSDC 100,000,000 -> 65,000,000 raw (35 devUSDC spent on the 70%-weighted leg, exactly as expected -- no scaling bug), Reserve Token 0 -> 49,595,879 raw minted, `legSources` correct. The same run also read the swap authority's REAL live balance: **0.2345 SOL** -- low enough that the new sufficiency check is not hypothetical; flagged as an operational follow-up (see Risks), not fixed by this pass (no protocol/trade-mechanics change). No Rust/program code touched; `docs/journey-map/` untouched throughout.
- **2026-07-30 (Thu), landing-page/wallet/DevNet-token-model correction pass (earlier the same day):** Audited the full DevNet token flow end to end per a product review. Found and fixed two real frontend bugs unrelated to the protocol itself: (1) the landing page's Featured Reserves and KPI strip read from a completely different, stale legacy store (`src/state/store.tsx`'s seed data) than Discover Reserves does, and the Featured card's link (`/reserve/:address`) pointed at a route that was never registered -- a silent dead link; re-sourced both from the same on-chain-verified `useAppStore` discovery store Discover uses, via a new shared `src/merge/lib/reserveCardProps.ts` (`buildReserveCardProps`/`selectFeaturedReserves`), added honest loading/unavailable/empty states, and dropped the KPI strip's "24h Volume" and "Reserve Token Holders" tiles since neither has a genuine data source (the latter's own type comment called it "Number of fictional holders"). (2) Portfolio's "Get DevNet Test Assets" button minted mockX/Y/Z directly to any connected wallet on demand -- a real faucet-like path for Reserve Assets the mandated token model forbids; removed it (DEC-0053) while leaving `api/devnet/mint-test-assets.ts` itself untouched, since it's also the legitimate seed-funding path `createReserveClient.ts` uses during genuine Reserve creation. Replaced the landing page's long "Create. Launch. Trade." / fee-table / "Transparent by construction" sections with a single mandated "How It Works" section, fixed the remaining "Browse Reserves" CTAs to read "Discover Reserves" (matching the header/footer, which already did), and replaced the wallet chip's click-to-disconnect behavior with a proper dropdown panel (new `src/components/WalletPanel.tsx`) showing abbreviated address+copy, a Solana DevNet badge, genuine SOL/devUSDC/Reserve-Token balances (reusing the existing `rpcResilience.ts` cache/dedup primitives, zero new polling), and an explicit Disconnect button. **Audit's central finding, deliberately left open (DEC-0054):** Buy still mints every non-devUSDC leg fresh from the server swap authority rather than from the user's own holdings, and Sell still always pays out SOL, not devUSDC -- both genuine on-chain instructions, neither a fabrication, but neither is the full devUSDC round-trip the product model calls for, and closing that gap requires either activating the still-dormant `ssr_devnet_amm` (DEC-0051) or another explicitly-approved architecture; no frontend-simulated workaround was added. Also fixed a real, pre-existing, unrelated bug found while running the test suite: `package.json`'s `test:program` script pointed `ts-mocha` at the wrong tsconfig (`tsconfig.node.json`, which doesn't even include `tests/**` and doesn't set `rootDir`), causing a hard `TS5011` failure before any test could run; repointed it at the already-present, already-correct `tests/tsconfig.json`. 9 new offline tests (`tests/phase_landing_wallet_corrections.ts`; 83/83 total passing). `tsc -b`, `vite build`, `oxlint` all clean. No Rust/program code touched; `docs/journey-map/` untouched throughout.
- **2026-07-30 (Thu), user-reported: "Could not refresh live Solana DevNet Reserves (429...)" banner on opening Discover (later the same day).** Traced to a real remaining gap in the prior RPC-resilience pass: `discoverAllReserves`'s very first call (`fetchProtocolConfig`) has no retry of its own (unlike every per-Reserve read inside it, which already has per-account try/catch) -- a single isolated 429 there, most likely to land right when `RealReserveSync` first mounts alongside everything else the page is loading, failed the ENTIRE discovery pass immediately and surfaced the error banner for what's usually just one unlucky request, then cleared itself on the next scheduled poll tick once it succeeded (exactly matching the user's report: "getting this message as soon as i open discover reserves. it then disappears"). Fixed by wrapping the discovery call in `withRateLimitRetry` (bounded, 3 attempts) at the `RealReserveSync.tsx` call site -- safe to retry the whole call since the failure happens at the very first line, before any per-Reserve work has run. 74/74 offline tests still pass (no new test needed -- this reuses the already-tested `withRateLimitRetry`); `tsc -b`, `vite build`, `oxlint` all clean. No Rust/program code touched.
- **2026-07-30 (Thu), follow-up: honest token-supply reads under transient RPC failure.** While extending the prior pass's live verification to also cover Sell, a real, separate bug surfaced: `packages/sdk/src/readOnly.ts`'s `fetchReserveOnChain` (used directly by both `api/devnet/swap-sign.ts`'s Buy-transaction-building path and `DTRDetail.tsx`'s post-trade refresh) silently mapped ANY `connection.getTokenSupply` failure -- including a transient 429 -- to a reported supply of `"0"`, indistinguishable downstream from a genuinely brand-new, unseeded Reserve. This made a real Buy against a real, fully seeded Reserve fail with a misleading `computeMintRequirements: zero supply -- Reserve must be seeded first` error purely because one RPC read hit a 429 under congestion. Fixed with a new bounded, rate-limit-only retry (`getTokenSupplyWithRetry`, mirroring `rpcResilience.ts`'s `withRateLimitRetry` pattern -- duplicated rather than imported since `packages/sdk` has no dependency on the frontend app) around that one call; the function's existing null-safety contract for a genuinely unreadable/nonexistent mint is unchanged. 3 new offline tests (`tests/phase_rpc_resilience.ts`, bringing the total to 74/74); `tsc -b`, `tsc -p packages/sdk`, `tsc -p scripts/tsconfig.json`, `tsc -p api/devnet/tsconfig.json`, `vite build`, `oxlint` all clean. **Live verification of this fix's companion goal (a real Sell through the same hardened path) could not be completed today**: 5 consecutive attempts against live DevNet each failed for a different, genuinely external reason (the exact 429 this pass targets, a faucet transaction expiring before confirmation under the same congestion) -- public DevNet RPC congestion was observed markedly worse today than during the prior day's successful Buy run. Not a regression or a known defect: Sell shares the identical, already offline-tested `signSubmitAndConfirm`/`confirmSignatureBounded` code path Buy uses (same function in `zapClient.ts`), and Sell has its own separate live evidence from earlier passes (DEC-0032, DEC-0043). Flagged transparently as still-open rather than claimed complete. No Rust/program code touched.
- **2026-07-29 (Wed), Buy/confirmation RPC-resilience pass (later the same day):** A follow-up user report ("why are buys hitting connection rate limits?") traced to a real contention bug the prior corrective pass hadn't covered: `RealReserveSync.tsx`'s discovery poll ran an unconditional `setInterval(15s)` forever (dozens of sequential RPC calls -- discovery + per-Reserve delegate lookups + balance reads -- every 15 seconds regardless of what else was happening), `DTRDetail.tsx` added its own uncached balance reads on mount and after every trade, and a Buy's own confirmation used `connection.confirmTransaction`'s websocket-subscription strategy, which was observed live throwing `ws error: Unexpected server response: 429` under congestion. **Fix, centralized in a new `src/merge/lib/rpcResilience.ts`** (reused by every caller instead of each inventing its own retry logic): `nextPollDelay` (adaptive backoff, doubles+jitters on a real 429 capped at 120s, steps back down gradually on success) replaces the fixed interval; `getCached`/`invalidateCached(Prefix)` (bounded-TTL cache + in-flight de-dup) replaces every ad hoc balance/discovery cache; `withReadConcurrencyLimit` caps concurrent reads at 4; `confirmSignatureBounded` replaces the websocket-based confirmation with bounded, retryable `getSignatureStatuses` polling that treats a 429 mid-poll as inconclusive (never a failure) and only reports `failed`/`expired` on a genuine on-chain answer, throwing a new `AmbiguousConfirmationError` (carrying the real signature) when bounded polling is exhausted with no definitive answer -- never fabricating success or failure. `RealReserveSync.tsx` now has an overlap guard (a slow pass is never joined by a second) and defers entirely while a new `useAppStore.txInFlight` flag (set for the duration of any Buy/Sell) is true, so the background poll never races a trade's own RPC traffic. `DTRDetail.tsx`'s Buy/Sell each track a `TxPhase` state machine (`canSubmitNewTransaction` gates re-submission; `txPhaseLabel` drives the exact required UX strings -- "Preparing transaction", "Waiting for wallet approval", "Submitted -- confirming on DevNet", "DevNet RPC is temporarily busy -- your transaction is still being verified"), catches `AmbiguousConfirmationError` to run a one-shot `reconcileByBalanceChange` re-check (a fresh, uncached real balance read -- reports confirmed only if the balance genuinely moved as expected) and shows a persistent pending-verification banner with the signature/Explorer link and a manual "Check status" retry otherwise; `refreshRealReserveNow` is unchanged in scope (still only the one Reserve that traded) but now invalidates+repopulates the shared cache instead of reading around it. `zapClient.ts`'s `signSubmitAndConfirm` still submits exactly once (never auto-retried) and now surfaces the signature via `onProgress` the instant `sendRawTransaction` succeeds, before confirmation even starts; `requestSignedZapTransaction` also learned to detect a rate limit from the response body's own error text, not just the HTTP status, after live testing surfaced that `api/devnet/swap-sign.ts`'s catch-all normalizes every internal error (including a genuine upstream 429 hit while building the transaction) to HTTP 500 -- a real, narrow bug in the SAME feature, fixed in the same file rather than left as a silent verification-blocker. 27 new offline tests (`tests/phase_rpc_resilience.ts`; 71/71 total passing across all phase test files); `npx tsc -b`, `npx tsc -p scripts/tsconfig.json`, `npx tsc -p api/devnet/tsconfig.json`, `npx vite build`, `npx oxlint` all clean. **Live-verified**: `scripts/verify_rpc_resilience_buy.ts` ran the exact `executeBuyZapDevUsdc` function DTRDetail.tsx calls (a disposable trader wallet standing in for a connected wallet, the closest available production-equivalent path -- no browser-automation tool exists in this environment) against the live Phase C devUSDC Reserve; after several earlier attempts were legitimately blocked by the same sustained public-RPC congestion this pass fixes (each attempt getting further, informing the fixes above), a clean run confirmed a real Buy end-to-end: signature `61xiFz6QwFmVoybDY7yeHF1bcbvukiXMJDN8UCPJa1FVUfFQy7HwV7hFtnEBQivhwp9PHNcAuW1UGMLPD3LzEiB7`, devUSDC balance 20,000,000 -> 16,500,000 raw (3.5 devUSDC genuinely spent on the 70%-weighted leg), Reserve Token balance 0 -> 4,975,956 raw minted, `legSources` correctly reporting the devUSDC leg as `user-devusdc-balance` and the mockX leg as `devnet-test-asset-faucet`, `onProgress` firing `awaiting-wallet` then `submitted` exactly as designed, final status `confirmed`. No Rust/program code touched this pass.
- **2026-07-29 (Wed), corrective DevNet data-integrity + Reserve-launch pass (session closeout follow-up, earlier the same day):** A live-testing user report (a legacy "Solana Blue Chips"/BLUE listing reporting a 25,000 USDC -> 1,125 BLUE "purchase", plus a Reserve-launch failure: `429: Too many requests for a specific RPC call`) triggered a full 7-stage discovery-and-fix pass. **Root cause of the BLUE incident:** a 100%-client-side fictional demo catalog (`DTR_SEEDS` in `seed-data.ts`, 6 fake DTRs incl. "Solana Blue Chips") plus simulated `buyDTRToken`/`sellDTRToken` store actions and an auto-seeded fake 25,000 USDC / 50,000 SSR wallet balance granted on every wallet connect -- confirmed nothing ever touched Phantom, no transaction signature exists, no devUSDC or any token moved, and no Reserve Tokens were ever minted; purely local arithmetic. **Fix:** removed the entire fictional catalog and all simulated Buy/Sell/create paths; the app now only ever discovers and displays genuine on-chain DevNet Reserves via a new fail-closed merge (`mergeDiscoveredReserves` in `onChainReserve.ts`) that drops any previously-known Reserve missing from a *fully verified* discovery pass, but correctly retains it if that pass itself had unresolved per-account issues (distinguishing "genuinely closed" from "transient RPC failure"); a new localStorage migration (persist `version` 4 -> 5) strips any legacy fictional DTR/holding/fake-balance still sitting in an existing tester's browser. **devUSDC (`Djn4aGJ3JTgqGpGdQFkmq73gG8KvkwRswP7pNaouuw4k`) is now the default DevNet settlement asset for Buy/mint**: a new `buildBuyZapInstructionsDevUsdc` SDK builder + `buy-devusdc` swap-sign action draws the devUSDC leg from the trader's own real wallet balance via a genuine `transfer_checked` (not a fabricated conversion), while any non-devUSDC leg is honestly labeled in the UI as DevNet-test-asset-faucet-funded, not simulated; direct proportional redemption remains the canonical, only Sell path, unchanged. **Root cause of the 429 launch failure:** `estimateCreateReserveCost` fired an uncached, undebounced 4-call rent-exemption RPC burst on every keystroke/slider-tick in the Create-Reserve form; fixed with a shared 10-minute rent-constant cache + in-flight request de-duplication (`getRentConstants`), a bounded exponential-backoff-with-jitter wrapper for genuine 429s only (`withRateLimitRetry`/`isRateLimitError`), a 400ms debounce on the cost-estimate effect, and an ambiguous-submission guard in `CreateDTR.tsx` that reads `ProtocolConfig.reserveCount` before and after a failed launch to distinguish "definitely nothing submitted" (safe to retry, shown verbatim) from "status unclear, do not retry" (shown instead of inviting a duplicate launch) -- the failed launch that prompted this pass left no partial on-chain state (confirmed: it failed during cost estimation, before any transaction was built or signed). **Reserve inventory:** confirmed 5 genuine, already-existing, actively-healthy on-chain Reserves already covered Stage 4's requirement with excellent compositional diversity (mockX/Y/Z + devUSDC + wrapped SOL all represented) -- no new Reserve needed to be created; see Recently Completed for the full list with addresses. Added 12 new offline tests (`tests/phase_data_integrity.ts`; 44/44 total passing), all confirmed clean: `npx tsc -b`, `npx tsc -p scripts/tsconfig.json --noEmit`, `npx tsc -p api/devnet/tsconfig.json --noEmit`, `npx vite build`, `npx oxlint`. **Live-verification caveat:** `scripts/verify_data_integrity_pass.ts` was run repeatedly against live DevNet; the public RPC endpoint was under severe, sustained congestion throughout this pass (the exact `429`/"Too many requests" condition being fixed), which itself validates the bug but also limited how much of the live chain could be re-confirmed in one run -- see Technical Health and Risks for exactly what was and wasn't completed live. No Rust/program code was touched this pass.
- **2026-07-29 (Wed), SDK + ManageDTR wiring for Phase F/G (later the same day):** Added `packages/sdk/src/managementInstructions.ts` (6 instruction builders, including `update_targets`, a pre-existing instruction never before wired to a real signed transaction from the browser) and `src/merge/lib/managementClient.ts` (browser sign-and-send wrapper). Wired `ManageDTR.tsx`: the Rebalance tab now submits a real `update_targets` transaction for on-chain Reserves; a new "Reserve Composition" card lists every asset with its live vault balance and offers Fund (any zero-balance asset) / Remove (only the last-registered zero-balance asset, mirroring the program's own eligibility rule) / Add Asset; a new "Wind Down" card in Overview offers Initiate Wind Down (Active) or Close Reserve (WindDown + zero supply + zero vault balances). Everything gated to the Reserve's root manager only -- discovered that no on-chain delegate signing path exists for ANY manager action in this app yet (canManageDelegates/canRebalance only ever check the local simulated delegate list), so extending delegate support is correctly deferred as a separate follow-up rather than built inconsistently for just the new actions. `npx tsc -b`, `npx vite build`, `npx oxlint` all clean; the existing 32/32 offline tests still pass. See DEC-0049.
- **2026-07-29 (Wed), Phase F/G deployed and live-verified (earlier the same day):** The user resolved the SOL shortfall with a real 5 SOL transfer to the deployer wallet (confirmed via `solana confirm`: 3.65985132 -> 8.65985132 SOL). Regenerated the IDL via `anchor idl build` (plain `anchor build` still panics on this machine's cargo-build-sbf toolchain detection -- a separate, pre-existing, unrelated issue; `anchor idl build` uses a different code path) and synced it into `packages/sdk/idl/ssr_protocol.json`. Deployed the upgrade (signature `W9w5pn9wTwHayWPbVXL89WKYTYDQZ39ZpY5jZefi9ZTVVTNZzjMiE1SYqC8cVZvNyKhStuiRQuZnS4d5wD8sDK8`; `solana program show` confirmed Data Length 647608 bytes, matching the built `.so` exactly). Wrote and ran `scripts/verify_phase_f_g.ts` against the live program with real signed transactions: `add_reserve_asset_active`/`remove_reserve_asset`/`fund_new_reserve_asset` exercised against the persistent Gate-9 fixture reserveOne (adding, removing, re-adding, and funding a 3rd asset, mockZ); a fresh disposable Reserve exercised through the full wind-down lifecycle -- `initiate_wind_down`, then confirmed `mint_reserve_tokens_in_kind` is rejected with the *exact* expected error (`0x177a` = 6010 = `UnexpectedReserveStatus`, not an incidental failure), then confirmed `redeem_reserve_tokens_in_kind` still succeeds during `WindDown` (the precise invariant this pass's `require_redemption_allowed` fix depends on), then `close_reserve`, with the Reserve/ReserveAsset/vault accounts all confirmed actually gone on-chain via `fetchNullable`/`getAccountInfo`. All 8 real transactions succeeded exactly as designed. See DEC-0048.
- **2026-07-29 (Wed), Phase F/G: composition management + wind-down, coded and verified offline; deploy blocked (earlier the same day):** Designed and implemented 5 new instructions (`add_reserve_asset_active`, `fund_new_reserve_asset`, `remove_reserve_asset`, `initiate_wind_down`, `close_reserve`) plus 2 new `ReserveStatus` variants (`WindDown`, `Closed`, Borsh-compatible with all 16 existing live Reserve accounts). Two real design corrections made by reading existing code before writing new code: (1) a simple `enabled=false` flag would not actually have stopped new deposits into a "disabled" asset, since `mint_reserve_tokens_in_kind`'s deposit math never reads `enabled`/`target_weight_bps` at all -- replaced with real, narrowly-scoped removal (last-registered + zero-balance only); (2) `redeem_reserve_tokens_in_kind`'s `require_active_or_paused()` didn't permit `WindDown`, which would have made `close_reserve`'s zero-supply requirement permanently unreachable -- fixed by extending and renaming it to `require_redemption_allowed`. See DEC-0046. `cargo check`/`cargo clippy` both pass clean (zero errors, zero new warnings). **Deployment blocked**: the larger binary needs ~4.51 SOL for its upgrade buffer + fee; the deployer wallet holds 3.65985132 SOL and the public DevNet faucet refused every retry (2/1/0.5/0.1 SOL, all rate-limited) this pass. See DEC-0047 and Blockers below. No on-chain state touched; the failed deploy attempt spent no funds.
- **2026-07-29 (Wed), Phase B: real devUSDC settlement token, faucet, and DevNet SOL onboarding (later the same day):** Created a real DevNet SPL-token mint ("SSR Test USD"/devUSDC, `Djn4aGJ3JTgqGpGdQFkmq73gG8KvkwRswP7pNaouuw4k`, 6 decimals, classic SPL Token, zero initial supply) reusing the existing Gate-9 manager/swap-authority keypair as mint+freeze authority -- a deliberate reuse decision needing zero new secrets. Built two new DevNet-only server endpoints (`api/devnet/faucet-devusdc.ts`, `api/devnet/sponsor-sol.ts`), each gated by a durable, live on-chain balance ceiling plus a best-effort cooldown, and each failing closed via a live genesis-hash check before touching the chain. Deduplicated the pre-existing per-file secret-loading helper across all four DevNet endpoints into shared `api/devnet/_lib/` modules. Added a `DevnetOnboarding` panel to the existing Portfolio page (no redesign, no new page) showing live devUSDC/SOL balances, claim buttons, real pending/confirmed/failed states, Explorer links, and a permanent guided fallback to the public DevNet faucet. Live-verified end-to-end via `scripts/verify_devusdc_faucet.ts` (calls the real deployed handlers, not a reimplementation) against a disposable test wallet: real faucet claim confirmed (+500 devUSDC), real SOL grant confirmed (+0.01 SOL), both immediate repeat claims correctly rejected (429/cooldown), Mainnet path confirmed impossible. `npx tsc -b`, `npm run build`, `oxlint` all pass; 20 new offline tests plus the pre-existing 12 all pass (32/32). See DEC-0042.
- **2026-07-29 (Wed), bounded live-DevNet verification of Phase A (later the same day):** Built a reusable, read-only verification script (`scripts/verify_discovery.ts`, zero transactions sent) that calls the exact same discovery functions the frontend uses, and ran it against live Solana DevNet. Confirmed cluster/program ID/genesis hash, enumerated all 16 existing on-chain Reserves (14 of which are not one of the 2 committed fixtures), and found **TestLo** for real: reserveId 13, ~1,004.975 Reserve Token supply, backed 100% by mint `2KBajm7Xufj8UaFQbKqLquhMRqeqjLZdDuXtoqYkSUgu` (confirmed to be "mockX"/"MOCX" from the fixture registry), with its name ("StrategicSolReserve") and ticker ("TESTLO") resolved directly from its own on-chain `metadataUri` -- no TestLo-specific code anywhere. Zero account-integrity issues across all 16 Reserves (owner/discriminator/PDA-derivation/cross-contamination/duplicate checks all passed). Found and fixed a real bug in the process: the verification script's own extra integrity checks weren't resilient to the public DevNet RPC's documented 429 rate-limiting and crashed on the first run; hardened both the script and `packages/sdk/src/discovery.ts` itself (per-account try/catch, a new non-breaking `issues` field) so a single bad account can never abort discovery of the rest -- confirmed live on the second run (16/16 succeeded). `npx tsc -b`, `npx vite build`, `oxlint`, and the existing offline test suite all pass clean. See DEC-0041 and `docs/project/DEVNET_IMPLEMENTATION_PLAN_2026-07-29.md`'s live-verification section for full detail.
- **2026-07-29 (Wed), Phase A of a new DevNet delivery plan:** Following same-day product/terminology decisions (final terminology: "Decentralized Token Reserve" = whole basket, "Reserve" = underlying holding, "Reserve Token" = the basket token, recorded in CLAUDE.md and DEC-0037), implemented Phase A of an 8-phase (A-H) plan to make SSR DevNet a fully on-chain testing environment (`docs/project/DEVNET_IMPLEMENTATION_PLAN_2026-07-29.md`). Built a canonical, on-chain-only Reserve discovery layer (`packages/sdk/src/discovery.ts`) that enumerates every Reserve the deployed program actually knows about via `ProtocolConfig.reserveCount` + PDA derivation (no `getProgramAccounts` needed), replacing a hardcoded 2-fixture list that made a real, user-created Reserve (handle "TestLo") invisible outside the browser that created it. Unified Discover/DTRDetail/Portfolio/ManageDTR on this layer, added clear "Live on Solana DevNet" vs. "Simulated Demo" labeling everywhere a Reserve renders, replaced the Sell tab's blended synthetic-SOL headline with the real in-kind redemption estimate (DEC-0039), and disabled (rather than silently faked) delegate-management and rebalance-execution actions on ManageDTR for real on-chain Reserves until their real signed-instruction support ships in later phases. `npx tsc -b`, `npm run build`, and `oxlint` all pass clean; added a new offline pure-logic test file (`tests/phase_a_discovery.ts`, 12/12 passing). Did not touch the deployed program, create any new token mint, or start Phase B+.
- **2026-07-28 (Tue), dashboard redesign (later the same day):** Redesigned `/internal/status` into an executive engineering dashboard: a whole-project completion metric re-weighted across 5 Engineering Areas (80%, up from the protocol-mission-only 10%), real milestone/timeline/infrastructure panels sourced from a new curated `docs/project/ENGINEERING_TIMELINE.md`, and a mechanically-generated, re-runnable repo-metrics file (`docs/project/REPO_METRICS.json` via `scripts/generate-repo-metrics.ts`) for real commit/contributor/branch/LOC facts (DEC-0036).
- **2026-07-28 (Tue), treasury upgrade deployed:** Completed DEC-0033's DevNet protocol-treasury upgrade once the user funded the deployer wallet directly: deployed the `update_protocol_config` program upgrade, set `ProtocolConfig.default_protocol_fee_destination` to the requested treasury, and verified real fee routing with a `collect_fees` call (treasury Reserve Token balance 0 -> 200 raw units) (DEC-0035).
- **2026-07-28 (Tue), corrective pass (earlier the same day):** Fixed the Create-Reserve-then-can't-Buy/Sell bug (dynamic on-chain Reserve/asset validation replacing a hardcoded 2-fixture allowlist, DEC-0029); added SOL/wrapped-SOL as a real Create Reserve asset (DEC-0030); reduced Create Reserve to 2-3 wallet approvals with a real rent-based Wallet Cost Summary shown before any signature (DEC-0031); wrote an `update_protocol_config` instruction to repoint the DevNet protocol treasury (DEC-0033, later deployed -- see above); added subtle DevNet-transaction status messaging to the Buy/Sell trade panel. Validated the whole pass end-to-end against a brand-new Reserve driven through the real client code (`scripts/verify_e2e_fresh_reserve.ts`), which caught and led to fixing a real USD-to-SOL conversion bug in the wrapped-SOL seed-funding math (DEC-0032). Discovered and logged two environment constraints: the public DevNet airdrop faucet is rate-limited, and `solana-test-validator`/`anchor test` do not run on this machine without Windows Developer Mode -- adopted localnet-by-default with batched DevNet upgrades as the go-forward policy (DEC-0034). `npm run build`, `npx tsc -b`, and `oxlint` all pass clean.
- **2026-07-28 (Tue), Gate 10-11 (later the same day):** Wired the existing SSR.fun frontend to the live DevNet program: real wallet-adapter connection (Phantom/Solflare/Backpack), real on-chain Reserve reads replacing mocked data for the Gate-9 fixtures, Buy/Sell redefined as a real SOL zap reusing the existing trade tab (DEC-0027), a DevNet-only server-side swap-authority co-signer (`api/devnet/swap-sign.ts`) and test-asset faucet (`api/devnet/mint-test-assets.ts`), and real Reserve creation through the existing Create-a-Reserve stepper for the 3 supported DevNet test assets. Verified every new on-chain code path against live DevNet with real transactions before wiring it into the UI (a full Buy/Sell round trip, a from-scratch Reserve creation, the swap-sign endpoint, and the faucet). Full production build (`tsc -b && vite build`) passes.
- **2026-07-28 (Tue):** **Protocol work:** installed Visual Studio Build Tools (user-approved) to close the BPF/SBF toolchain gap; ran real `cargo-build-sbf`, producing a genuine deployable `.so`; generated a DevNet deployer wallet; worked around a fully-exhausted public airdrop faucet (confirmed via explicit 429 error, both CLI and web3.js paths) by having the user fund the wallet directly; **deployed SSR Protocol to Solana DevNet** and verified the deployment on-chain; applied `cargo fmt`; added two more adversarial tests (repeated-init, excess-redemption) to the test suite. Also ran `cargo check`/`cargo build`/`cargo clippy` clean (zero errors/warnings) before the SBF build, from the toolchain installed the previous day. **Later the same day:** generated a real IDL via `anchor idl build`; fixed a chain of ESM/CJS module-resolution issues to get the Anchor test harness running outside `anchor test`'s local-validator default; replaced faucet-dependent `requestAirdrop` calls with direct transfers from the funded deployer wallet; found and fixed two real test bugs (a stale-snapshot assertion, a singleton-init test incompatible with persistent-network state); **ran the full test suite against the live DevNet program for the first time -- 14 passing, 0 failing.**
- **2026-07-27 (Mon):** [Dashboard work, same day, see below] Inspected the full SSR frontend as product spec; cloned and analyzed Reserve Protocol's Folio reference repo; locked SSR's account/authority/instruction architecture; wrote the complete v1 Anchor program source (17 instructions) and a TypeScript SDK; wrote an Anchor test suite; installed a native Rust/MinGW/Solana-CLI toolchain and used it to compile the program for the native host target, finding and fixing ~15 real compiler-caught bugs; generated a real DevNet program keypair; fixed a real security gap in `collect_fees`. Dashboard work (same day, earlier): migrated hosting to Vercel, connected the custom domain, hardened the testing deployment as unlisted, built the password-protected `/internal/status` dashboard, and fixed its login-lockout bug (DEC-0008).
- **2026-07-24 (Fri):** Unified Reserve card components across the homepage and Discover page, added a real (simulated) wallet-connect modal, expanded the Portfolio page, and completed a terminology pass to align all copy on "Reserve" / "Launch Reserve."
- **2026-07-23 (Thu):** Rebuilt the homepage to match the approved reference design, ported the Discover / Create / Portfolio / Manage / Reserve-detail flows from the SSR.FUN-MERGE prototype, added the eagle seal logo and favicon, and fixed layout issues in the Create-a-Reserve stepper.
- **2026-07-22 (Wed):** No recorded repository activity.

## Recently Completed
- **Helius RPC integration + Buy 429/"insufficient SOL" diagnosis pass (2026-07-30).** Helius is now the primary DevNet RPC provider (DEC-0055): every `api/devnet/*.ts` handler resolves its Connection through a new shared `resolveRpcUrl()`, and the browser's Connection is pointed (production/preview builds only) at a new narrow, method-allowlisted, payload-validated server-side proxy (`api/devnet/rpc-proxy.ts`) instead of ever holding a raw provider URL/API key -- `HELIUS_RPC_URL` was added to the linked Vercel project via `vercel env add`, never committed. Diagnosed the reported 50-devUSDC Buy failures: devUSDC's decimals/scaling/fee-payer were all already correct; the real causes were (1) `swap-sign.ts`'s `fetchReserveOnChain` call sitting outside its own try/catch, letting a raw 429 escape unformatted (confirmed live -- the public endpoint returned that exact string this session), and (2) no sufficiency check on the swap authority's own SOL balance, which funds Buy's wrapped-SOL leg and 100% of Sell's payout -- a low balance there produced a transaction Phantom's simulation rejects for a signer OTHER than the connected wallet, misreported generically as "insufficient SOL" (DEC-0056). Both fixed; also migrated the two remaining websocket-based `connection.confirmTransaction` call sites to the already-proven bounded `confirmSignatureBounded` (DEC-0057), required for the new HTTP-only proxy. 17 new offline tests (100/100 total); `tsc -b`, `vite build`, `oxlint` all clean. **Live-verified** via new `scripts/verify_helius_buy_fix.ts`: a real 50-devUSDC Buy genuinely through Helius -- signature `hGDWXhLtLFHrsJssUd7YzQxXcyY4oQJdqVjTxLVMoNsrYCqHMdxJK1zcDNHvJTvunYkJr13j5Acr1fQacTLj1Sa`, trader SOL spent 0.004089 (fee+rent only), devUSDC 100,000,000 -> 65,000,000 raw, Reserve Token 0 -> 49,595,879 raw minted. Also read the swap authority's real live balance: **0.2345 SOL** -- confirming the new check is not hypothetical (see Risks). No Rust/program code touched.
- **Landing-page/wallet/DevNet-token-model correction pass (2026-07-30).** Featured Reserves + KPI strip re-sourced onto the same on-chain-verified discovery store Discover Reserves uses (new shared `src/merge/lib/reserveCardProps.ts`), fixing a dead Featured-card link and dropping two KPI tiles with no genuine data source. Portfolio's mockX/Y/Z faucet-like "Get DevNet Test Assets" button removed (DEC-0053) without touching the genuine Reserve-creation seeding path that shares its API endpoint. Landing page's long marketing copy/fee-table replaced with a mandated "How It Works" section; remaining "Browse Reserves" CTAs fixed to "Discover Reserves". Wallet chip now opens a real dropdown panel (new `src/components/WalletPanel.tsx`) instead of disconnecting on click, showing genuine SOL/devUSDC/Reserve-Token balances via the existing RPC-resilience cache. Central audit finding -- Buy still mints non-devUSDC legs from the swap authority rather than the user's holdings, and Sell always pays SOL, not devUSDC -- deliberately left open rather than patched with a workaround; see DEC-0054. Also fixed `package.json`'s `test:program` script, which pointed at the wrong tsconfig and made the entire offline test suite fail to even start. 9 new offline tests (83/83 total); `tsc -b`, `vite build`, `oxlint` all clean. No Rust/program code touched.
- **Honest token-supply reads under transient RPC failure (2026-07-30).** Fixed `packages/sdk/src/readOnly.ts`'s `fetchReserveOnChain` silently reporting a real Reserve's supply as `"0"` on a transient `getTokenSupply` RPC failure (indistinguishable from a genuinely-unseeded Reserve, and observed to make a real Buy fail with a misleading error) -- new bounded rate-limit-only retry (`getTokenSupplyWithRetry`) fixes it without changing the function's existing null-safety contract. 3 new offline tests (74/74 total); all typechecks/build/lint clean. A follow-up live Sell verification (closing the one gap the prior pass's Buy-only run left open) could not complete today after 5 attempts, each blocked by a different symptom of markedly worse public DevNet RPC congestion than the day before -- not a known defect (Sell shares Buy's already-verified code path and has separate earlier live evidence). No Rust/program code touched.
- **Buy/confirmation RPC-resilience pass (2026-07-29).** Fixed the real contention between `RealReserveSync.tsx`'s background discovery poll, `DTRDetail.tsx`'s balance reads, and Buy/Sell's own submission+confirmation -- the actual answer to "why are buys hitting rate limits" after the prior pass only addressed Create-Reserve's launch burst. New shared `src/merge/lib/rpcResilience.ts`: adaptive poll backoff (`nextPollDelay`), cache+dedup (`getCached`/`invalidateCached(Prefix)`), a 4-concurrent-read cap (`withReadConcurrencyLimit`), and bounded polling-based confirmation (`confirmSignatureBounded`, replacing the websocket-based `confirmTransaction` observed live throwing `ws error: Unexpected server response: 429`) that throws `AmbiguousConfirmationError` (never a fabricated result) when polling is exhausted without a definitive on-chain answer. `RealReserveSync` gained an overlap guard and pauses entirely while a new `useAppStore.txInFlight` flag is true; `DTRDetail`'s Buy/Sell each track a `TxPhase` state machine driving the required UX strings, reconcile an ambiguous outcome via one real (uncached) balance re-check, and show a persistent signature/Explorer-link pending-verification banner with manual "Check status" otherwise -- never inviting a duplicate submission while the first is unresolved. Also fixed a real bug surfaced only by live testing: `api/devnet/swap-sign.ts`'s catch-all normalizes every internal error to HTTP 500, including a genuine upstream 429 -- `zapClient.ts` now also detects a rate limit from the response body's own error text, not just the HTTP status. 71/71 offline tests pass (27 new, `tests/phase_rpc_resilience.ts`); `tsc -b`, both `scripts`/`api` tsconfigs, `vite build`, `oxlint` all clean. **Live-verified**: `scripts/verify_rpc_resilience_buy.ts` called the exact `executeBuyZapDevUsdc` function DTRDetail.tsx uses (closest available production-equivalent path -- no browser-automation tool exists in this environment) against the live Phase C devUSDC Reserve; after several attempts were legitimately blocked by the same sustained public-RPC congestion this pass targets (each getting further and directly informing the fixes above), a clean run produced a real confirmed Buy -- signature `61xiFz6QwFmVoybDY7yeHF1bcbvukiXMJDN8UCPJa1FVUfFQy7HwV7hFtnEBQivhwp9PHNcAuW1UGMLPD3LzEiB7`, devUSDC 20,000,000 -> 16,500,000 raw, Reserve Token 0 -> 4,975,956 raw, `legSources` and `onProgress` phases exactly as designed. No Rust/program code touched. `docs/journey-map/` untouched throughout.
- **Corrective DevNet data-integrity + Reserve-launch pass (2026-07-29).** Purged the fictional/mock demo catalog and every simulated Buy/Sell/wallet-balance path (root cause of the reported "25,000 USDC -> 1,125 BLUE" incident -- confirmed zero on-chain activity, ever); the app now only discovers and displays genuine on-chain DevNet Reserves, fail-closed. devUSDC (`Djn4aGJ3JTgqGpGdQFkmq73gG8KvkwRswP7pNaouuw4k`) is now the default DevNet settlement asset for Buy/mint, with a real transfer for the devUSDC leg and honest faucet-funded labeling for any other leg. Fixed the reported `429` Reserve-launch failure at its root (uncached/undebounced rent-cost RPC burst) with caching, de-duplication, bounded backoff, debouncing, and an ambiguous-submission guard against duplicate launches. Confirmed 5 genuine, distinct, already-healthy on-chain Reserves satisfy Stage 4's requirement (no new Reserve needed): **DevNet Reserve One** (`GFP9nJQyFWurTkJCEYYkBxjksUQUXLt9i3ZoUDncTy5C`, mockX 60%/mockY 40% at fixture creation, live-confirmed with 3 registered assets after later Phase F/G composition testing added a 3rd), **DevNet Reserve Two** (`H1U22fK3fMfsmz1WirJ4H63xBDcTEHgXjtSzw73tEfcJ`, mockX 40%/mockY 30%/mockZ 30%, 3 assets), **StrategicSolReserve "TestLo"** (`Hj8uifcUHAmTpwySQJgfo4F6B8Y68X2b48BmTKv89xSX`, 100% mockX, reserve_id 13, the real user-created Reserve found by Phase A discovery), **Phase C devUSDC Reserve** (`HAaoBxSVAnaxEusxxYUnPpAJAyjRzti4zuqxrLWYS4VE`, 70% devUSDC/30% mockX), and the **E2E corrective-pass Reserve** (`BuHRWKzzXQXhjL3WCsmHTT7qDooh2437DvXuxyExpiWg`, from DEC-0032's original fresh-Reserve verification, 2 assets) -- all 5 individually reconfirmed live and `active` via direct `fetchNullable` account reads on 2026-07-29 despite severe same-day public-RPC congestion. 44/44 offline tests pass (12 new); `tsc -b`, both `scripts`/`api` tsconfigs, `vite build`, and `oxlint` all clean. Live end-to-end Buy/Sell/fresh-launch verification through the exact UI code path was attempted repeatedly against live DevNet but could not be driven to full completion in one run due to sustained public RPC congestion (the same condition this pass fixes) -- partial live evidence obtained instead (see Technical Health): all 5 Reserves reconfirmed, and a real devUSDC faucet-funding transaction landed with a real signature before the run's buy-submission step itself hit repeated 429s and aborted safely (no partial state; the abort occurred before/during a fresh throwaway-wallet transaction, never touching existing protocol state). No Rust/program code changed. `docs/journey-map/` untouched throughout.
- **Session closeout + DEC-0045 architecture decision (2026-07-29).** DEC-0045 (open decision on a real on-chain DevNet swap mechanism) was resolved: DEC-0051 records the decision to build a dedicated, separate, DevNet-only constant-product AMM program (`ssr_devnet_amm`), structurally isolated from `ssr_protocol` (own program ID, disjoint PDA namespace, zero shared accounts or CPI). Implementation was started (program written, `cargo check`/`clippy` clean, deployed to DevNet at `AJbXGWSU1x9LtJW7uRKJCwS3JZYqXwqHXpX6erY7dS6c`) but is **incomplete** -- never initialized (no `AmmConfig`, no pools, no liquidity), never wired into the SDK-facing frontend, never verified end-to-end. Deliberately deferred rather than rushed: preserved on branch `experimental/devnet-amm-swap` (commit `d6d1979`), clearly labeled experimental/incomplete/DevNet-only/not production-ready, and explicitly NOT merged into `main`. `main` itself is untouched by this -- it only gained the DEC-0045/DEC-0051 decision-log entry (docs-only, commit `61440f2`) on top of the already-deployed Phase F/G state. Direct proportional redemption remains the canonical Sell path; no unfinished AMM control is exposed anywhere in the live site.
- **SDK + ManageDTR UI wiring for Phase F/G (2026-07-29).** Composition management (add/fund/remove asset, real `update_targets`) and wind-down (initiate/close) are now real, signed, root-manager-only actions in the live website, not just a Node verification script. See DEC-0049.
- **Phase F/G: composition management + wind-down, deployed and live-verified (2026-07-29).** 5 new instructions (`add_reserve_asset_active`, `fund_new_reserve_asset`, `remove_reserve_asset`, `initiate_wind_down`, `close_reserve`), 2 new `ReserveStatus` variants, 3 new errors, 5 new events, one existing method extended/renamed (`require_redemption_allowed`). Deployed via a real program upgrade and exercised end-to-end with 8 real signed transactions against live DevNet -- see DEC-0046/DEC-0048.
- **Phase E: Jupiter DevNet feasibility -- proven infeasible (2026-07-29).** Real API evidence: Jupiter's aggregator has zero DevNet awareness (returns real Mainnet-only quotes regardless of mint). Flagged as a genuine, unresolved product-decision blocker (whether/how to build a real on-chain swap mechanism) rather than building an AMM unilaterally -- see DEC-0045 for the decision needed. Does not block Phase F's composition-management or Phase G.
- **Phase D: fee/treasury verification for the devUSDC path (2026-07-29).** Confirmed fee accrual/collection is composition-agnostic by design (no protocol change needed). Live `collect_fees` call against the Phase C Reserve routed 800 pending manager + 200 pending protocol Reserve Token shares correctly. See DEC-0044.
- **Phase C: devUSDC as a real Reserve asset, live-verified (2026-07-29).** Zero protocol changes needed -- devUSDC added to the existing real-asset allowlists (CreateDTR, mint-test-assets faucet, swap-sign zap, discovery candidate mints). Live-verified via `scripts/verify_devusdc_reserve.ts`: a fresh 70% devUSDC / 30% mockX Reserve created, seeded, Bought, and Sold, all real signatures/confirmations. See DEC-0043.
- **Phase B: real devUSDC settlement token + faucet + DevNet SOL onboarding, live-verified (2026-07-29).** Real DevNet mint created and verified (`Djn4aGJ3JTgqGpGdQFkmq73gG8KvkwRswP7pNaouuw4k`); real faucet + sponsorship endpoints built, tested, and live-verified against a disposable test wallet (real signatures, real confirmed balance changes, real cooldown rejection). Zero new required secrets. See DEC-0042.
- **Live-DevNet verification of Phase A discovery (2026-07-29).** `scripts/verify_discovery.ts` (read-only, zero transactions) confirmed the canonical discovery layer works against the deployed program: all 16 existing Reserves enumerated (not just the 2 fixtures), zero integrity issues, and TestLo/MOCX genuinely found via general discovery (reserveId 13, ~1,004.975 supply, backed by mintX/"MOCX", name/ticker resolved from its own on-chain metadataUri). See DEC-0041.
- **Phase A of the new A-H DevNet delivery plan (2026-07-29): canonical on-chain Reserve discovery + mock-state purge.** `packages/sdk/src/discovery.ts` enumerates every deployed Reserve directly from `ProtocolConfig.reserveCount` (no `getProgramAccounts`); `RealReserveSync.tsx` runs it on a poll and merges results by real on-chain address; Discover/DTRDetail/Portfolio/ManageDTR are unified on one source of truth with real-vs-simulated Reserves clearly badged; the Sell tab shows real in-kind redemption estimates instead of a blended synthetic-SOL figure (DEC-0039); ManageDTR's delegate CRUD and rebalance-execute are disabled (not faked) for real on-chain Reserves pending Phase D/F. Full record and remaining Phase B-H scope in `docs/project/DEVNET_IMPLEMENTATION_PLAN_2026-07-29.md`; terminology finalized in DEC-0037; architecture in DEC-0038.
- **Gate 1 (research):** Full frontend inspection + Reserve Protocol Folio analysis (`docs/protocol/RESERVE_REFERENCE_ANALYSIS.md`, `FRONTEND_INTEGRATION.md`).
- **Gate 2 (architecture lock):** `SSR_ARCHITECTURE.md`/`ACCOUNT_MODEL.md`, DEC-0009 through DEC-0019.
- **Gates 3-7 (program): written, compiled, linted, and deployed.** `cargo check`/`cargo build`/`cargo clippy` all pass with zero errors/warnings (native host target). Real BPF/SBF compilation (`cargo build-sbf`) succeeded after installing Visual Studio Build Tools, producing a genuine 538,056-byte deployable `.so`.
- **Gate 8: SSR Protocol is deployed and live on Solana DevNet.** Program ID `2dURvmSdHeyaFES5rxaE1zgPSHCBLW5BLNguJ2Tu1mkW`, deployment signature `arj6tzkUCqgSeJBs9inJv3nLcyoK5smasygGEZmDZ5hk84QukwLsJKR9mbaZryJz56uFRTkeXoGtTFSBSMWuaju`, verified on-chain via `solana program show` (correct upgradeable-loader owner, correct authority, correct data length, rent-exempt balance matching the exact `solana rent` estimate). Full record in `docs/protocol/DEVNET_RUNBOOK.md`.
- **Gate 9 (core): the full test suite has executed against the live DevNet program -- 14 passing, 0 failing.** Real Reserve creation, two-asset registration, seeding, proportional mint/redeem across two holders, pause/unpause, a scope-restricted delegate, and the cross-Reserve vault-substitution isolation test (the plan's most security-critical test) all confirmed working on-chain, not just typechecked. Full record in `DEVNET_RUNBOOK.md`'s "Test execution record."
- **Gate 9 (fixtures): persistent, documented DevNet fixtures now exist**, built via `scripts/devnet_fixtures.ts` -- a root manager, 2 restricted delegates with distinct permission scopes, 2 holders, a 2-asset Reserve and a 3-asset "multi-asset" Reserve (both seeded, both with real proportional holder balances), and a pause/unpause cycle demonstrating both Active and Paused states. Full record in `docs/protocol/DEVNET_FIXTURES.md`. Building this also surfaced a real, confirmed limitation of the public DevNet RPC endpoint: it fully blocks `getProgramAccounts` (403) and rate-limits heavily under sustained same-day use (429) -- the fixture script is checkpoint-resumable as a result.
- ~15 real compiler-caught bugs fixed (Anchor lifetime-elision across all handlers, an Anchor-1.0 `CpiContext` API change, a missing Cargo feature, a macro glob-re-export requirement) -- see `DEVNET_RUNBOOK.md`.
- A real security gap fixed: `collect_fees`'s `protocol_fee_destination` now validated against `ProtocolConfig`.
- Two more adversarial tests written (repeated-init, excess-redemption), plus the previously-stubbed cross-Reserve vault-substitution test (the most important one in the plan) fully implemented -- all now passing live, not just written.
- Two real test bugs found and fixed only by actually running against live state: a stale pre-transaction snapshot assertion, and a singleton-init test that assumed resettable local-validator state rather than persistent DevNet state.
- **Gate 6 (SDK):** `packages/sdk` typechecks clean against real installed packages.
- **Gate 10-11 (frontend integration): real wallet connection, real on-chain reads, real Buy/Sell zap, and real Create Reserve are all wired into the existing frontend with its design unchanged.** See `docs/protocol/FRONTEND_INTEGRATION.md` for the full technical record and DEC-0027 for the architecture decision. Verified against live DevNet via `scripts/verify_zap.ts` (Buy 0.005 SOL then Sell half the resulting balance against the Reserve One fixture -- vault balances, Reserve Token supply, and both parties' SOL all moved exactly as computed) and `scripts/verify_create_reserve.ts` (a brand-new Reserve created/registered/seeded from a fresh keypair with zero prior state, confirmed `active` on-chain).
- Full documentation set under `docs/protocol/`, kept current with the verified state throughout.
- Dedicated branch `protocol/devnet-v1`, pushed to origin; prior uncommitted dashboard work preserved untouched throughout.
- (Unrelated) Dashboard: password-gated `/internal/status` with configurable login rate limiting (DEC-0008).
- **Corrective pass (2026-07-28): dynamic Reserve/asset validation, SOL as a real asset, signature reduction + cost transparency, subtle DevNet status messaging, and a real end-to-end fresh-Reserve verification script -- see DEC-0029 through DEC-0032 and `scripts/verify_e2e_fresh_reserve.ts`.** Fresh Reserve `BuHRWKzzXQXhjL3WCsmHTT7qDooh2437DvXuxyExpiWg` (Reserve Token mint `AQ6i33grhTZk3vA9Ph4paGGnsdFR9nif7trEaK1aNJ2d`) created, seeded, Bought from, and Sold from with real signatures and no hardcoded registration anywhere in the path.
- **DevNet protocol treasury (DEC-0033/DEC-0035) is deployed and verified live.** `update_protocol_config` instruction shipped via a real DevNet program upgrade (signature `JuNiHri3m5wuCwv7aKaYHnMLvoMSEPUuJjCehainCrZRUg6RqfZMdUeVxosxjbAzn9hxSFc8nThXcoVGEx9BvVK`); `ProtocolConfig.default_protocol_fee_destination` set to `EME96L9JK7VQvMg76txApB8Kb9npdyUfFcpQKDqYupmq` (signature `2cEtFTEPa5qiEWdPWZ16bTVdQaDZ7XyhUK6zjJpwUKkLvKYwwE8fD11gseoyZHgVRvRthD1d6spzy8VaYRZGcmTC`); verified with a real `collect_fees` call (signature `3k4KKk9EdiuNigWgSjqWK3kiigedAKAP2ifmkX1cMXAzxE4wTk5F8jj5bbGE6sPaqUA2TEKSX11RLjRAbPFxw9Ww`) that paid the treasury 200 raw Reserve Token units (previously 0) and the Reserve's manager 800 raw units, matching the pending fee shares accrued by DEC-0032's Buy exactly.

## In Progress
- Live re-verification of Sell through the RPC-hardened path (`scripts/verify_rpc_resilience_buy.ts`, extended to attempt Sell after Buy) -- 5 attempts on 2026-07-30 each failed on a different symptom of unusually severe public DevNet RPC congestion (worse than the day before), never on the same code path twice. Non-blocking -- Sell uses the exact same `signSubmitAndConfirm`/`confirmSignatureBounded` code path already live-verified via Buy, and has its own separate live evidence from earlier passes (DEC-0032, DEC-0043).
- Gate 10-11 (pre-existing): no browser-automation tool was available in this environment to drive an actual Phantom click-through -- see Risks.
- `ssr_devnet_amm` (DEC-0051): deliberately paused, incomplete, isolated on branch `experimental/devnet-amm-swap` (commit `d6d1979`) -- not on `main`, not deployed to production, not active on DevNet (deployed bytecode exists but was never initialized). Not available for testing.

## Next Actions
0. Consider provisioning a dedicated/paid DevNet RPC endpoint (the existing `VITE_SOLANA_RPC_URL` environment pattern already supports pointing at one, client-safe by design) -- the public endpoint's sustained congestion this pass (and the prior one) is now client-side-mitigated (caching/dedup/backoff/adaptive polling/bounded confirmation) but a dedicated endpoint would still meaningfully improve real-world responsiveness under heavy testing traffic.
1. Wire on-chain delegate signing (not just root-manager signing) for manager actions generally -- discovered this pass that no manager action in this app (new Phase F/G ones or pre-existing ones like `update_targets`/`pause_reserve`) currently supports a real on-chain delegate's signature; `canManageDelegates`/`canRebalance` only check the local simulated delegate list. A proper fix touches those shared helpers plus every gated action, not just the newest ones.
2. Finish `ssr_devnet_amm` (DEC-0051's decision, DEC-0045 resolved) if/when this work is resumed -- currently incomplete and parked on branch `experimental/devnet-amm-swap` (commit `d6d1979`): the program is written and deployed to DevNet but never initialized/seeded, and SDK/frontend wiring for Sell's optional redeem-and-swap and Phase F's rebalance trade execution has not been started.
3. Get a real browser + Phantom to click through the full flow at least once (connect, view fixtures, Buy, Sell, Create Reserve, and now the DevNet onboarding panel) -- everything below the UI layer is verified against live DevNet (including production, this pass), but the literal click path has not been human/browser-tested.
4. Before any restricted beta: migrate the upgrade authority off the current single dev-controlled key to a multisig (DEC-0015). This now also covers the devUSDC mint/freeze authority and the SOL sponsorship signer, which reuse the same key (see DEC-0042's documented tradeoff).
5. Monitor DevNet SOL budget on the shared manager/swap-authority wallet (~0.391 SOL as of the end of Phase B, before this pass's own transactions) -- this wallet now serves four roles (Buy/Sell zap co-signer, test-asset faucet, devUSDC mint authority + faucet, SOL sponsor) drawing from one balance; will need periodic top-ups under sustained testing use. Note the public DevNet RPC also confirmed-blocks `getProgramAccounts` (403) and rate-limits heavily under sustained same-day use (429) -- a dedicated/paid RPC endpoint would materially help here, and would also lift Phase A's documented per-Reserve asset/delegate discovery limitation (see `FRONTEND_INTEGRATION.md` "Canonical discovery").
6. Enable Windows Developer Mode on this machine (or run elevated) so `solana-test-validator`/`anchor test` can actually run, per DEC-0034's localnet-by-default policy -- currently blocked by a Windows privilege requirement (`SeCreateSymbolicLinkPrivilege`).
7. Consider adding a lightweight automated test harness for the new integration logic (`packages/sdk/src/zapInstructions.ts`, `createReserveFlow.ts`) beyond the live-DevNet verification scripts -- Phase A/B added the first frontend/SDK/API-side offline unit tests (`tests/phase_a_discovery.ts`, `tests/phase_b_devusdc.ts`, via the existing mocha/chai/ts-mocha toolchain, no new test framework installed), but broader frontend test coverage remains a pre-existing gap.

## Engineering Areas

<!--
  Whole-project completion view for the /internal/status dashboard's hero
  metric -- deliberately a DIFFERENT weighting than "Roadmap" below.
  "Roadmap" tracks the current Solana-protocol mission's 8 gates/phases
  only, which under-weights the real, already-shipped SSR.fun simulation
  platform (the entire homepage/Discover/Create/Portfolio/Manage/Reserve-
  detail product, gated at just 10/100 of Roadmap's weight because Roadmap
  is scoped to "the protocol mission", not "the whole project"). This block
  re-weights across the project's actual 5 major engineering areas so the
  headline number represents the entire project. Each area's completion is
  computed from the Roadmap phases it maps to (see mapping below) --
  recompute both blocks together; never hand-edit either completion number
  independently of what it's derived from.

  Mapping:
    - Product / Simulation Platform  = Roadmap phase 1
    - Reserve Protocol                = Roadmap phases 2+3+4 (weighted)
    - Frontend Integration            = Roadmap phase 5
    - Security & Testing              = Roadmap phase 6
    - Production Readiness            = Roadmap phases 7+8 (weighted)
-->

```json
[
  { "area": "Product / Simulation Platform", "weight": 20, "completion": 1.00, "status": "done", "detail": "Homepage, Discover, Create Reserve, Portfolio, Manage, Reserve-detail -- full simulation-mode product." },
  { "area": "Reserve Protocol", "weight": 25, "completion": 0.98, "status": "in_progress", "detail": "23-instruction Anchor program live on Solana DevNet, including Phase F/G composition management + wind-down (deployed and live-verified with real signed transactions, DEC-0046/DEC-0048); architecture and research 100% done." },
  { "area": "Frontend Integration", "weight": 20, "completion": 0.99, "status": "in_progress", "detail": "Real wallet connection, real on-chain reads, real Buy/Sell zap (devUSDC-settled by default, with adaptive-backoff discovery polling and bounded-polling confirmation replacing the websocket strategy), real Create Reserve with launch-reliability hardening, a canonical fail-closed on-chain discovery layer, and real signed composition-management + wind-down actions in ManageDTR (Phase F/G, DEC-0049) -- root-manager-only for now, on-chain delegate signing deferred as a follow-up." },
  { "area": "Security & Testing", "weight": 20, "completion": 0.89, "status": "in_progress", "detail": "14/14 protocol tests passing live on DevNet; 71/71 frontend/SDK offline tests passing (27 new this pass covering adaptive polling, cache/dedup/concurrency limiting, bounded confirmation outcomes, duplicate-submission gating, and balance-change reconciliation); a full fresh-Reserve E2E lifecycle AND a real live Buy through the RPC-hardened path both verified end-to-end with real signatures; a corrective pass removed all remaining simulated/fictional trading state from the UI. No external audit yet." },
  { "area": "Production Readiness", "weight": 15, "completion": 0.00, "status": "not_started", "detail": "Frontend is genuinely live in production; the PROTOCOL itself is DevNet-only -- needs a security audit and upgrade-authority multisig migration (DEC-0015) before Mainnet." }
]
```

## Roadmap

```json
[
  { "phase": 1, "name": "Frontend Foundation", "weight": 10, "completion": 1.0, "status": "done" },
  { "phase": 2, "name": "Protocol Research and Gap Analysis", "weight": 10, "completion": 1.0, "status": "done" },
  { "phase": 3, "name": "Solana Reserve Architecture", "weight": 15, "completion": 1.0, "status": "done" },
  { "phase": 4, "name": "Core Solana Program", "weight": 20, "completion": 0.96, "status": "in_progress" },
  { "phase": 5, "name": "Client and Frontend Integration", "weight": 15, "completion": 0.99, "status": "in_progress" },
  { "phase": 6, "name": "DevNet Testing and Security Validation", "weight": 15, "completion": 0.89, "status": "in_progress" },
  { "phase": 7, "name": "Restricted Beta", "weight": 10, "completion": 0.0, "status": "not_started" },
  { "phase": 8, "name": "Production Readiness", "weight": 5, "completion": 0.0, "status": "not_started" }
]
```

## Blockers
- **(Resolved 2026-07-29) Phase F/G program upgrade was blocked on a DevNet SOL shortfall** -- the user resolved it with a direct 5 SOL transfer to the deployer wallet; the upgrade is deployed and live-verified. See DEC-0047 (blocker) and DEC-0048 (resolution).
- **`solana-test-validator`/`anchor test` do not run on this machine** without Windows Developer Mode enabled (`ERROR_PRIVILEGE_NOT_HELD` on ledger symlink creation) -- blocks fully adopting DEC-0034's localnet-by-default policy until resolved. Direct `cargo check`/`cargo-build-sbf` remain available as a fallback and were used to validate DEC-0033's new instruction before its deployment.
- The previously-tracked BPF/SBF toolchain gap (Visual Studio Build Tools) and the original DevNet SOL funding gap are both resolved -- see Decision Log DEC-0023/DEC-0024. The DevNet protocol treasury upgrade's funding gap is also resolved (the user transferred 3 SOL to the deployer wallet) and the upgrade itself is deployed and verified -- see DEC-0035.

## Dependencies
- Vercel (hosting, deployment, custom domain, and the environment variable used by the internal dashboard). **`DEVNET_SWAP_AUTHORITY_SECRET_KEY`** (a Sensitive/encrypted production env var) is now a hard runtime dependency for `api/devnet/swap-sign.ts`, `api/devnet/mint-test-assets.ts`, and (new, Phase B) `api/devnet/faucet-devusdc.ts`/`api/devnet/sponsor-sol.ts` -- Buy/Sell, Create-seeding, the devUSDC faucet, and DevNet SOL onboarding all depend on this one variable being set.
- **Helius (dedicated Solana DevNet RPC provider), new as of 2026-07-30 (DEC-0055).** `HELIUS_RPC_URL` (Sensitive/encrypted, Production+Preview; Non-sensitive on Development -- Vercel's Sensitive tier is Production/Preview-only) is now the primary RPC endpoint for every server-side DevNet action and, via the new `api/devnet/rpc-proxy.ts`, for the browser's Connection in production/preview builds. The public `api.devnet.solana.com` endpoint remains as a bounded, read-only fallback inside the proxy only, and as the still-hardcoded default in `scripts/verify_*.ts` other than the new `verify_helius_buy_fix.ts`.
- Name.com (domain registrar and DNS for `strategic-super-reserve.fun`).
- **Solana DevNet -- now an active runtime dependency, not just a target.** Program is deployed and live there; the frontend now reads/writes it directly. The real devUSDC mint (`Djn4aGJ3JTgqGpGdQFkmq73gG8KvkwRswP7pNaouuw4k`, Phase B) is also a live DevNet-only dependency with zero real value.
- Reserve Protocol's `reserve-index-dtf` reference repository (read-only, external to this repo) -- reference material only, not a runtime dependency.
- The public Solana DevNet airdrop faucet is unreliable from this environment (confirmed daily-limited, and confirmed rate-limited again this session across multiple retry attempts -- most recently a full lockout across 2/1/0.5/0.1 SOL requests, all refused, while attempting to fund the Phase F/G program upgrade) -- further DevNet SOL needs either the user's own wallet/faucet access or a direct transfer to the deployer wallet (`6idsSUE6u7fqHg6edrdMEjNTnG62wyCANAsJ2YBmeuHk`, 3.65985132 SOL as of the Phase F/G pass, ~0.85 SOL short of that upgrade's buffer + fee cost -- see DEC-0047), Phase B's new tightly-limited sponsored-SOL grant (`api/devnet/sponsor-sol.ts`), or the swap-authority/fixture-manager wallet directly (`Ef7vbQghn7Fc4LzUnyJsvov1f5f9aRSfWksiaSmWpquj`, **live-confirmed 0.2345 SOL as of the 2026-07-30 Helius/Buy-fix pass** -- see Risks).

## Risks
- **(Elevated, 2026-07-30) The swap-authority wallet's DevNet SOL balance is live-confirmed at 0.2345 SOL** -- low enough that a Buy against any Reserve with a wrapped-SOL leg, or almost any Sell (which pays out 100% of the sale in SOL from this same wallet), could plausibly fail the new `assertSwapAuthorityHasSol` sufficiency check (DEC-0056) today. This wallet funds four separate duties (every Sell's full payout, Buy's wrapped-SOL legs, devUSDC faucet claims' ATA rent, SOL sponsorship grants) and is also the devUSDC mint/freeze authority. Before this pass, a low balance here surfaced as a confusing Phantom "insufficient SOL" error misattributed to the connected wallet; now it fails closed with an honest, correctly-attributed error instead -- but the underlying operational need (top up this wallet) is unchanged and now more urgent given the live-read balance. **Needs a top-up soon.**
- **The public DevNet RPC endpoint (`api.devnet.solana.com`) is confirmed unreliable under sustained same-day use**: it fully blocks `getProgramAccounts` (403 "Your IP or provider is blocked from this endpoint") and rate-limits heavily (429) under repeated sequential calls -- confirmed again this session (a direct request during this pass's own investigation returned a raw `429` immediately). **Update (2026-07-30, Helius integration pass):** Helius is now the primary RPC provider everywhere (server-side directly, browser via the new `api/devnet/rpc-proxy.ts`), which should substantially reduce reliance on the public endpoint's capacity -- the public endpoint remains only as the proxy's bounded fallback. Combined with the 2026-07-29 pass's client-side mitigations (adaptive backoff, cache/dedup, concurrency cap, bounded polling-based confirmation), this is the strongest mitigation yet, though Helius's own DevNet-tier rate limits are a new dependency to monitor under heavier testing traffic.
- **No real browser + Phantom click-through has been performed** -- no browser-automation tool was available in this environment. Every on-chain code path (wallet-adapter wiring, the zap transactions, Create Reserve) was verified against live DevNet via direct scripts using a throwaway keypair standing in for a connected wallet, and all modules were confirmed to load/transform cleanly through the Vite dev server, but the literal UI click path is unverified by a human or automated browser.
- **No automated test suite exists for the dashboard/frontend either** (pre-existing risk, unchanged; the new integration logic has live-DevNet verification scripts instead, see `scripts/verify_*.ts`).
- **Main JS bundle exceeds 500 kB** (pre-existing, now larger with the Solana web3/wallet-adapter/Anchor libraries bundled client-side for the first time).
- **The general testing site relies on obscurity, not authentication** (see Decision Log DEC-0004/DEC-0005, unchanged, pre-existing).
- **Rebalance trade execution (an actual Jupiter-route CPI) does not exist on-chain at all in v1** (deliberate, per DEC-0017/DEC-0021). Separately, the Buy/Sell zap's SOL<->asset leg uses a DevNet-only fixed-price swap authority, not a real router -- see `FRONTEND_INTEGRATION.md`'s Mainnet-readiness notes below.
- **The upgrade authority is a single dev-controlled key** (`6idsSUE6u7fqHg6edrdMEjNTnG62wyCANAsJ2YBmeuHk`, DevNet only) -- must migrate to a multisig before any restricted beta, per DEC-0015.

## Mainnet-Readiness Gaps (Gate 10-11 additions)
- **Swap routing**: the DevNet swap authority (fixed test pricing, mint-authority-based asset issuance) must be replaced with a real routing provider (e.g. Jupiter) before Mainnet -- isolated behind `packages/sdk/src/zapInstructions.ts`'s interface so the frontend itself needs no changes, only the instruction-building internals.
- **Asset allowlist**: Buy/Sell/Create-seeding currently only work for the 3 DevNet fixture test mints plus wrapped SOL (the swap authority's own mint authority and its own real SOL balance are what make this possible) -- a production allowlist needs a real asset-onboarding process instead. The allowlist check itself is now dynamic/per-asset-mint rather than per-Reserve-address (DEC-0029), which is the right shape for Mainnet too -- only the specific mints in the allowlist need to change.
- **Oracle/pricing**: `SOL_TEST_PRICE_USD` and the $1-per-fixture-asset constants are fixed testing values, not a price feed -- Mainnet needs Pyth (or similar) for any displayed USD value; core mint/redeem accounting itself never depends on price data (unchanged from the original protocol design).
- **Slippage protection**: the zap's `maxAssetAmounts`/`minAssetAmountsOut` use a flat 2% buffer computed client/server-side, not a user-configurable slippage tolerance.
- **Transaction atomicity**: already production-shaped (single atomic transaction per Buy/Sell), no gap here.
- **Security review/audit**: not started; DevNet-only testing infrastructure (the swap authority, faucet) has zero exposure to real value and would need to be removed entirely, not merely reviewed, before Mainnet.

## Decisions Required
- Should the general testing site move from "unlisted" to real authentication before it's shared with anyone outside the immediate team? (pre-existing, unchanged)
- **(Resolved, DEC-0045/DEC-0051)** The swap-mechanism decision is made: a dedicated, separate, DevNet-only constant-product AMM (`ssr_devnet_amm`), isolated from `ssr_protocol`. Implementation is incomplete and deliberately deferred -- preserved on branch `experimental/devnet-amm-swap` (commit `d6d1979`), not merged, not active. Remaining decision, if/when resumed: whether to finish it now or keep deferring.
- **(Open, DEC-0054, 2026-07-30)** Buy's non-devUSDC legs are still minted fresh by the swap authority rather than sourced from the user's own holdings, and Sell still always pays out SOL rather than devUSDC -- neither is the genuine devUSDC round-trip the product model calls for. Closing this requires an explicit decision: (a) finish and wire in `ssr_devnet_amm` (DEC-0051) to provide a real devUSDC<->mockX/Y/Z conversion, or (b) approve a different architecture. No workaround was added in its place.

## Technical Health
- **Frontend typecheck:** passing (`tsc -b`), now covering real Solana/wallet-adapter/Anchor integration code across `src/`, `packages/sdk/`, and `api/devnet/` (the latter isolated into its own tsconfig to avoid a monorepo module-system conflict between the frontend's bundler resolution and Node's stricter ESM/CJS rules -- see FRONTEND_INTEGRATION.md). `src/merge/lib/` gained the same CommonJS-scoping `package.json` this pass so its client code (`createReserveClient.ts`, `zapClient.ts`) could be exercised directly from a Node verification script (DEC-0032).
- **Frontend build:** passing (`vite build`), re-verified after this pass's changes.
- **SDK typecheck (`packages/sdk`):** passing (`npx tsc --noEmit`, zero errors); gained `readOnly.ts` (live on-chain reads), `zapInstructions.ts`/`zapPricing.ts` (Buy/Sell zap, now with wrapped-SOL wrap/unwrap and `usdToSolLamports`), `createReserveFlow.ts` (real Reserve creation), and a checked-in `idl/` + `fixtures/` (the committed IDL and DevNet fixture registry the frontend imports).
- **Protocol test-suite typecheck (`tests/`, `tsconfig.tests.json`):** passing (`npx tsc --noEmit`, zero errors).
- **Protocol Rust build (native host target):** passing, zero errors, zero warnings (`cargo check -p ssr_protocol`, re-verified this pass after adding `update_protocol_config`).
- **Protocol Rust build (BPF/SBF target, `cargo build-sbf`):** **passing and deployed** -- the `update_protocol_config` instruction is live on DevNet (548,296-byte `.so`, up from 538,056 bytes) -- see DEC-0035.
- **Protocol lint (`cargo clippy`):** passing, zero warnings as of the last full run (pre-this-pass; re-run recommended before the next DevNet upgrade).
- **Protocol format (`cargo fmt`):** applied (pre-this-pass).
- **Rust/Anchor tests:** last executed against the live DevNet program -- 14 passing, 0 failing (`npx mocha --require ts-node/register tests/ssr_protocol.ts` against `https://api.devnet.solana.com`). See `DEVNET_RUNBOOK.md`'s "Test execution record." `anchor test`/`solana-test-validator` do not currently run on this machine (Windows Developer Mode required, DEC-0034); no new Rust-level test was added for `update_protocol_config` as a result -- validated instead via `cargo check` + `cargo-build-sbf` + reuse of the already-proven `has_one = authority` / `NotProtocolAuthority` pattern.
- **Lint (frontend):** `oxlint` clean (only pre-existing warnings; no errors), re-verified after this pass's changes.
- **CI:** none configured; deploys are manual and intentional (see Decision Log DEC-0001).
- **New on-chain integration logic verified against live DevNet** (not just typechecked) via `scripts/verify_reads.ts`, `verify_zap.ts`, `verify_swap_sign_endpoint.ts`, `verify_create_reserve.ts`, `verify_mint_test_assets.ts`, `verify_dynamic_reserve.ts` (DEC-0029), and `verify_e2e_fresh_reserve.ts` (DEC-0032, a full fresh-Reserve create/Buy/Sell lifecycle through the real client code) -- see `FRONTEND_INTEGRATION.md` for what each confirms.
- **Phase A (2026-07-29): `packages/sdk/src/discovery.ts`'s canonical discovery logic is typechecked, unit-tested offline, AND now live-DevNet-verified.** `tests/phase_a_discovery.ts` (12/12 passing, offline, mocha/chai/ts-mocha) plus `scripts/verify_discovery.ts` (read-only, zero transactions, run against the real deployed program on 2026-07-29): all 16 existing on-chain Reserves enumerated, zero integrity issues, TestLo/MOCX found via general discovery. See DEC-0041.
- **Buy/confirmation RPC-resilience pass (2026-07-29): typechecked, unit-tested offline, AND live-DevNet-verified with a real confirmed Buy.** `tests/phase_rpc_resilience.ts` (27/27 passing offline: adaptive poll-delay stepping, cache/dedup/invalidation, bounded read concurrency, `confirmSignatureBounded`'s confirmed/failed/expired/unknown outcomes including recovery through mid-poll 429s, duplicate-submission gating, `AmbiguousConfirmationError`, and balance-change reconciliation) brings the combined offline suite to 71/71. `npx tsc -b`, `npx tsc -p scripts/tsconfig.json --noEmit`, `npx tsc -p api/devnet/tsconfig.json --noEmit`, `npx vite build`, `npx oxlint` all pass clean. **Live verification via `scripts/verify_rpc_resilience_buy.ts`** (calls the real, unmodified `executeBuyZapDevUsdc` from `zapClient.ts` -- the exact function DTRDetail.tsx's Buy button calls -- against the live Phase C devUSDC Reserve, a disposable trader wallet standing in for a connected wallet): after several attempts were legitimately blocked by the same sustained public-RPC congestion this pass targets (informing the `swap-sign.ts` 500-vs-429 fix and the funding-step hardening added to the script itself), a clean run produced a real, fully confirmed Buy -- signature `61xiFz6QwFmVoybDY7yeHF1bcbvukiXMJDN8UCPJa1FVUfFQy7HwV7hFtnEBQivhwp9PHNcAuW1UGMLPD3LzEiB7`, real devUSDC balance 20,000,000 -> 16,500,000 raw, real Reserve Token balance 0 -> 4,975,956 raw, `legSources` correctly reporting the devUSDC leg as `user-devusdc-balance` and the mockX leg as `devnet-test-asset-faucet`, `onProgress` firing `awaiting-wallet` then `submitted` exactly as designed. No Rust changed this pass; `git status` on `programs/`/`Cargo.toml`/`Cargo.lock` confirmed empty.
- **Corrective data-integrity pass (2026-07-29): typechecked, unit-tested offline, AND partially live-DevNet-verified.** `tests/phase_data_integrity.ts` (12/12 passing offline, mocha/chai/ts-mocha: fail-closed merge exclusion/retention/price-history-preservation, RPC-429 recognition/backoff/give-up, devUSDC-to-Reserve-Token math) brings the combined offline suite to 44/44. `npx tsc -b`, `npx tsc -p scripts/tsconfig.json --noEmit`, `npx tsc -p api/devnet/tsconfig.json --noEmit`, `npx vite build`, and `npx oxlint` all pass clean. **Live verification via `scripts/verify_data_integrity_pass.ts` (real transactions, not a reimplementation) was attempted across 6 runs against live DevNet, all under severe, sustained public-RPC congestion** (repeatedly reproducing the exact `429`/"Too many requests" error this pass fixes -- itself corroborating evidence, not just a testing inconvenience). Achieved live: all 5 genuine Reserves individually reconfirmed `active` via direct `fetchNullable` reads (multiple runs); a real devUSDC faucet-funding transaction landed with a real signature. Not achieved in a single completed run: a full real Buy -> Sell -> fresh-launch chain end-to-end -- the closest run aborted safely mid-Buy-submission after repeated 429s, before any protocol state was touched (a disposable throwaway trader keypair, no user funds or existing Reserve state at risk). No Rust changed this pass; `git status` on `programs/`/`Cargo.toml`/`Cargo.lock` confirmed empty, so the previously-verified Rust build/lint/format state (see above) stands unchanged.
- **Phase B (2026-07-29): devUSDC mint + faucet + SOL onboarding are typechecked, unit-tested offline, AND live-DevNet-verified.** `tests/phase_b_devusdc.ts` (20/20 passing, offline) plus `scripts/verify_devusdc_faucet.ts` (calls the real deployed `api/devnet/faucet-devusdc.ts`/`api/devnet/sponsor-sol.ts` handlers, not a reimplementation, against a disposable test wallet): real mint verified live (owner/decimals/authorities/supply all confirmed), a real faucet claim (+500 devUSDC) and a real SOL grant (+0.01 SOL) both confirmed on-chain with real signatures, both immediate repeat claims correctly rejected. `npx tsc -p api/devnet/tsconfig.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, and `npx tsc -p scripts/tsconfig.json --noEmit` all pass clean. See DEC-0042.
- **Helius RPC integration + Buy 429/"insufficient SOL" diagnosis pass (2026-07-30): typechecked, linted, unit-tested offline, AND live-DevNet-verified with a real confirmed Buy genuinely through Helius.** `tests/phase_helius_rpc_and_buy_fix.ts` (17/17 new, offline: `resolveRpcUrl` precedence Helius > SOLANA_RPC_URL > public fallback; the rpc-proxy's method allowlist correctly includes every method this app's Connection genuinely calls and excludes `getProgramAccounts`/`requestAirdrop`; the per-IP sliding-window throttle allows/blocks/resets/isolates-by-key correctly; devUSDC's 6-decimal scaling for 50 devUSDC -> exactly 50,000,000 raw units, ruling out both a no-scaling and a 9-decimal-scaling regression; `sumWrappedSolLegLamports` sums only a wrapped-SOL leg and returns 0 for an all-token Reserve; `SwapAuthorityLowSolError`/`ZapBuildError` carry the right distinguishing codes) brings the combined offline suite to **100/100**. `tsc -b`, `tsc -p api/devnet/tsconfig.json --noEmit`, `tsc -p scripts/tsconfig.json --noEmit`, `vite build`, `oxlint` all clean. **Live verification via new `scripts/verify_helius_buy_fix.ts`** (calls the real, unmodified `executeBuyZapDevUsdc` -- the exact function DTRDetail.tsx's Buy button calls -- with its Connection built through the same `resolveRpcUrl()` server-side code now uses, genuinely routing through Helius): bought 50 devUSDC (the exact reported amount) against the live Phase C Reserve -- signature `hGDWXhLtLFHrsJssUd7YzQxXcyY4oQJdqVjTxLVMoNsrYCqHMdxJK1zcDNHvJTvunYkJr13j5Acr1fQacTLj1Sa`, trader SOL 0.050000 -> 0.045911 (0.004089 spent on ATA rent + fee only -- never the devUSDC amount, confirming the connected wallet was never the real constraint), devUSDC 100,000,000 -> 65,000,000 raw (35 devUSDC on the 70%-weighted leg, exactly as expected), Reserve Token 0 -> 49,595,879 raw minted, `legSources` correct, no 429 encountered. The same run read the swap authority's real live balance (0.2345 SOL) via the new `assertSwapAuthorityHasSol` code path, confirming that check now has genuine, current operational relevance (see Risks). No Rust/program code touched; `git status` on `programs/`/`Cargo.toml`/`Cargo.lock` confirmed empty.
- **Landing-page/wallet/DevNet-token-model correction pass (2026-07-30): typechecked, linted, and unit-tested offline; live DevNet verification not performed this pass (no funded signer/browser-automation available in this environment).** `tests/phase_landing_wallet_corrections.ts` (9/9 new, offline: `selectFeaturedReserves` never selects a simulated DTR regardless of its AUM, orders/slices genuine on-chain Reserves correctly; `buildReserveCardProps` derives correct premium/discount sign and topAssets ordering; mockX/Y/Z remain registered as genuine Reserve Assets and stay distinct from devUSDC) brings the combined offline suite to **83/83**. Also fixed a real, pre-existing bug found while running this suite: `package.json`'s `test:program` script passed `ts-mocha` the wrong tsconfig (`tsconfig.node.json`, whose `include` doesn't even cover `tests/**` and which doesn't set `rootDir`), causing a hard `TS5011` failure before a single test could execute regardless of any change in this pass -- repointed at the already-present, already-correct `tests/tsconfig.json`. `tsc -b`, `vite build`, `oxlint` all clean. This pass touched only `src/` (Home.tsx, Discover.tsx, Shell.tsx, Portfolio.tsx, new WalletPanel.tsx/reserveCardProps.ts), `src/index.css`, `tests/`, and `package.json` -- `git status` on `programs/`/`Cargo.toml`/`Cargo.lock` confirmed empty. No automated browser/DOM verification was performed (no `chromium-cli`/Playwright available in this environment and installing one was out of this pass's scope); verified instead via full production build, lint, offline tests, and a dev-server smoke check confirming every changed module compiles/transforms cleanly.

## Environment Status
- **Local:** `npm run dev` (Vite dev server) for the frontend -- now requires `DEVNET_SWAP_AUTHORITY_SECRET_KEY` set locally (`.env.local`, see `.env.example`) for Buy/Sell/Create-seeding to work; read-only pages (Discover, DTRDetail without trading) work without it. Protocol code has a fully working local build environment for both native and BPF/SBF targets (Rust 1.97.1, GNU toolchain at `C:\devtools\mingw64`, MSVC toolchain via Visual Studio Build Tools, Solana CLI 4.1.2 at `C:\devtools\solana`). **`solana-test-validator`/`anchor test` do not currently work on this machine** (Windows Developer Mode required -- DEC-0034); direct `cargo check`/`cargo-build-sbf` remain fully functional.
- **Development:** Vercel Preview deployments for the frontend, unchanged.
- **DevNet: SSR Protocol is live, its test suite passes against it, and the frontend now reads/writes it directly.** Program ID `2dURvmSdHeyaFES5rxaE1zgPSHCBLW5BLNguJ2Tu1mkW`, now a 23-instruction program including Phase F/G's `add_reserve_asset_active`/`fund_new_reserve_asset`/`remove_reserve_asset`/`initiate_wind_down`/`close_reserve` (upgraded and live-verified this pass, DEC-0046/DEC-0048) -- see `docs/protocol/DEVNET_RUNBOOK.md` for the deployment/test record and `FRONTEND_INTEGRATION.md` for the frontend integration record. This corrective pass's dynamic-Reserve-validation, SOL-asset, signature-reduction, and cost-transparency fixes are all frontend/API-side and were already live against the pre-upgrade deployment with no program upgrade needed. `ProtocolConfig.default_protocol_fee_destination` is now `EME96L9JK7VQvMg76txApB8Kb9npdyUfFcpQKDqYupmq`, verified both by a direct account read and a real `collect_fees` payout (DEC-0035).
- **Production: `https://strategic-super-reserve.fun` is live with the Helius RPC integration + Buy 429/"insufficient SOL" diagnosis pass** on top of the landing-page/wallet/DevNet-token-model correction pass, the Buy/confirmation RPC-resilience pass, Phase F/G's composition-management/wind-down UI, and everything from Phases A-D. Latest deploy: commit `398af79` on `main` via `vercel --prod` (deployment ID `dpl_CbsDMJid687zH3671LbRbMAXM1hE`, target `production`, aliased to `ssr-fun.vercel.app`/`strategic-super-reserve.fun`/`www.strategic-super-reserve.fun`). **New production environment variable:** `HELIUS_RPC_URL` (Sensitive, Production+Preview+Development, added via `vercel env add` before this deploy -- see DEC-0055). Verified post-deploy: both the custom domain and the Vercel alias return 200; a direct POST to the live `/api/devnet/rpc-proxy` correctly rejected a disallowed method (`getProgramAccounts`, JSON-RPC `-32601`) and correctly forwarded an allowed one (`getLatestBlockhash`, returning a genuine, fresh on-chain blockhash) without ever touching Helius for the rejected call; the served JS bundle (`main-DVnQPa7w.js`) contains zero occurrences of the raw Helius API key or `helius-rpc.com` and does reference the `rpc-proxy` path, confirming the key never reaches the browser. **Not yet verified: an actual browser + Phantom click-through** (no browser-automation tool was available in this environment) -- the underlying RPC/Buy-fix logic was live-verified via `scripts/verify_helius_buy_fix.ts`'s real signed Buy (see Recently Completed) through the exact client code the browser calls; the literal click path remains unverified by a human or automated browser.

## Last Updated
2026-07-30 (Helius RPC integration + Buy 429/"insufficient SOL" diagnosis pass. Adopted Helius as the primary DevNet RPC provider server-side (a new shared `resolveRpcUrl()`) and, via a new narrow server-side proxy (`api/devnet/rpc-proxy.ts`), for the browser -- the API key is a server-only secret (`HELIUS_RPC_URL`, added directly to the linked Vercel project via `vercel env add`, never committed) and is never bundled into any `VITE_*` variable. Diagnosed the reported 50-devUSDC Buy failures: devUSDC's decimals/scaling/fee-payer were already correct; the real causes were `swap-sign.ts`'s `fetchReserveOnChain` call sitting outside its own try/catch (letting a raw 429 escape unformatted -- reproduced live this session) and no sufficiency check on the swap authority's own SOL balance (which funds Buy's wrapped-SOL leg and all of Sell's payout), whose low real balance produced a transaction Phantom's simulation rejects for a signer other than the connected wallet, misreported generically as "insufficient SOL". Both fixed with a new `assertSwapAuthorityHasSol` check and structured `{error, code}` responses the client classifies into accurate messages; also migrated the two remaining websocket-based `confirmTransaction` call sites to the already-proven bounded `confirmSignatureBounded`, required for the new HTTP-only proxy. 17 new offline tests (100/100 total); `tsc -b`, `vite build`, `oxlint` all clean. **Live-verified** with a real 50-devUSDC Buy genuinely through Helius (signature `hGDWXhLtLFHrsJssUd7YzQxXcyY4oQJdqVjTxLVMoNsrYCqHMdxJK1zcDNHvJTvunYkJr13j5Acr1fQacTLj1Sa`) -- also read the swap authority's real live SOL balance (0.2345 SOL, low enough to be a genuine near-term operational risk, flagged in Risks). `docs/journey-map/` untouched throughout. Commit/deployment recorded in a same-session follow-up entry once pushed/deployed.)

2026-07-30 (Landing-page/wallet/DevNet-token-model correction pass. Audited the full DevNet token flow (faucet, Buy/mint, Sell/redeem, vault/TVL accounting) per a product review. Fixed two real, independent frontend bugs: the landing page's Featured Reserves/KPI strip were sourced from a stale legacy store disconnected from Discover Reserves' real on-chain discovery (including a dead Featured-card link to an unregistered route) -- re-sourced both onto the same `useAppStore` discovery data via a new shared `reserveCardProps.ts`, with honest loading/unavailable/empty states and two KPI tiles dropped for having no genuine data source; and Portfolio's "Get DevNet Test Assets" button was a reachable faucet-like path handing out mockX/Y/Z on demand, which the mandated token model forbids -- removed (DEC-0053), leaving the legitimate Reserve-creation seeding path that shares its API endpoint untouched. Replaced the landing page's long marketing copy/fee table with a mandated "How It Works" section; fixed remaining "Browse Reserves" CTAs to "Discover Reserves"; replaced the wallet chip's click-to-disconnect with a real dropdown panel (new `WalletPanel.tsx`) showing genuine SOL/devUSDC/Reserve-Token balances via the existing RPC-resilience cache and an explicit Disconnect button. **Central audit finding, deliberately left open rather than patched (DEC-0054):** Buy still mints non-devUSDC legs from the swap authority instead of the user's own holdings, and Sell always pays out SOL, not devUSDC -- both genuine on-chain instructions, but not the full devUSDC round-trip the product model calls for; closing it needs an explicit decision on `ssr_devnet_amm` (DEC-0051) or another approved architecture, not a frontend workaround. Also fixed a real, pre-existing, unrelated bug: `package.json`'s `test:program` script pointed at the wrong tsconfig, failing before any test could run regardless of this pass's changes -- repointed at the correct one. 9 new offline tests (83/83 total); `tsc -b`, `vite build`, `oxlint` all clean; no Rust/program code touched. No browser-automation tool was available in this environment, so interactive click-through/live-DevNet transaction verification was not performed this pass -- verified instead via full build/lint/test plus a dev-server compile check of every changed module. `docs/journey-map/` untouched throughout.)
