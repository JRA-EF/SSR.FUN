# DevNet checklist: claimant-only Manager fee collection, instant Protocol fee transfers, time-weighted TVL settlement (2026-08-14 pass)

Tester: automated session (Claude Code), 2026-08-14. Environment: Solana DevNet, program `2dURvmSdHeyaFES5rxaE1zgPSHCBLW5BLNguJ2Tu1mkW`, RPC via the public DevNet endpoint (`HELIUS_RPC_URL` was not available in this session -- see "Known gaps" below). Program upgrade signature: `WGkDTY31YPqewUcw5doaH2L5Gpd7D4NhYAcTboW5RjYhdp3WwaX99KzmdYAE5RPiXTV3jwHcT9Fa3cNMRTMo2KY` (slot 483836553, data length 874,952 bytes, up from 828,848 before this pass). Full raw run transcript: `docs/protocol/DEVNET_CLAIMANT_ONLY_TVL_SETTLEMENT_VERIFY_OUTPUT_2026-08-14.txt`. Verification scripts: `scripts/verify_claimant_only_and_tvl_settlement_2026_08_14.ts`, `scripts/verify_keeper_local_2026_08_14.ts`.

All items below are marked Passed only on the basis of real, signed, confirmed DevNet transactions -- never compilation, mocks, or offline tests alone (those are recorded separately as a precondition, not as evidence for these items).

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 1 | Root Manager can claim only its own accrued fees | **Passed** | Reserve `76XnH3FYcjQSDC6YSp6R1dUifcX1ypNS4jjfTshZsPiX` (reserveId 65). Manager collected its own 84,000-unit balance, signature `3aFHiEPW7HPxHMb1DDS35fonGpNG5TzGLyxAJyVapFnn8khoJo5Ry1EAhdcLnTjXQcS48NUxPm77trfAhEhAqUTA`; recipientB's pending balance (56,000) confirmed unchanged by the same read. |
| 2 | A recipient can claim only its own -- root Manager cannot claim another recipient's share | **Passed** | A transaction with `recipient = recipientB` but signed only by the Manager was rejected with "Signature verification failed" (Anchor's `Signer` constraint on `recipient` makes this structurally impossible, not just a runtime check). recipientB then independently self-collected its own exact 56,000-unit balance, signature `53RxdvoZu26rn48CxXBg65ACe6LcZnqQYKB6SMBq57k84b9EdbfvZAD9pND7mrmaovemCTLaoXeyyWhDFj6Hs2o2`. |
| 3 | Each Collect button/pending state operates independently (no cross-recipient interference) | **Passed (structural on-chain) + code review (frontend)** | On-chain: collecting the Manager's share left recipientB's pending balance byte-for-byte unchanged (item 1); the reverse also held. Frontend: `ManageDTR.tsx`'s Manager Fee Recipients list now tracks `collectingRecipient` (a single wallet address, not the page-wide `onChainTxPending` flag) so clicking one row's Collect button cannot disable or show "Confirming..." on any other row -- verified by source read, not interactively clicked in a browser this pass (see "Known gaps"). |
| 4 | Claimable/collected values update after confirmation | **Passed** | Every collect above was followed by a fresh on-chain `ManagerFeeRecipients` account read showing `pendingFeeShares` zeroed and `collectedFeeShares` incremented for the claiming recipient only. |
| 5 | Duplicate claims / repeated submission while pending rejected | **Passed** | An immediate second collect attempt by the Manager (pending balance already 0 from item 1) was rejected with `NoPendingFees` (error `require!(pending > 0, ...)` in `collect_manager_fee_share.rs`). |
| 6 | Initial Reserve creation (seed) applies the mint fee | **Passed** | `seed_reserve` transaction `5VJWu1gCpkqXWJSFRC81sKpt163GWes1FYmheCHrj2qS9hp2bksn12hrsm9y6dwM9H6eJDSKHMZWstDqHkgpuqTq` on a 10,000,000-unit gross seed at 2% configured mint fee: 100,000 units to treasury (Protocol) + 60,000/40,000 to the two recipients (Manager/recipientB), matching the SSR.fun formula exactly. |
| 7 | Protocol mint fees reach treasury in the SAME transaction as the mint, for every mint including the initial seed | **Passed** | Treasury Reserve Token ATA balance read immediately before and after each of 3 separate mint transactions (seed + 2 subsequent Buys) in this single script run: 0 -> 100,000 (seed, tx above), +40,000 (subsequent mint, tx `2gQMKkU5mKGug4wB4saPFbc2Qrux92KhuUdi1aTqTTrksSp6hXA6xrRNLRzEHVuUfgcV6vjGvJoUbGTMAcWK1J1Z`), and a third mint later in the run -- each delta landed exactly within that mint's own confirmed transaction, never a separate follow-up transfer. |
| 8 | No Protocol fee remains claimable after minting | **Passed** | `Reserve.feeConfig.pendingProtocolFeeShares` read as exactly `0` after every mint in this run (seed and both subsequent Buys) -- the "Send to Treasury Now" pending-balance concept no longer applies to any new mint activity. |
| 9 | Manager mint-fee shares accrue correctly | **Passed** | Both recipients' `pendingFeeShares` matched the exact largest-remainder apportionment of the Manager's 60/40 split after every mint (items 1, 6-7's transactions). |
| 10 | Weekly TVL fees use time-weighted average TVL and actual elapsed time (not latest-snapshot/52) | **Passed** | `TvlAccrual.periodSupplySeconds` (a genuine `supply x elapsed_seconds` cumulative accumulator) read as `701,300,000` immediately before settlement -- independently recomputed as `avg_supply x elapsed_seconds x annualized_rate / 31,536,000` from the account's own history and matched the on-chain settled amount exactly (protocol +2, manager +1 raw units on a deliberately tiny window/max 10%/yr rate, chosen so the effect is observable within seconds rather than requiring a real calendar week). Settlement tx `4UhY2HpMJZyWMMomKbZaMZPaQsKNtjorA1m5Fw3GkBAhpEicWBiLkwWnQr4MwASqwXbuFLgzeUWCN35K7bX2gG3F`. |
| 11 | TVL changes mid-week produce the correct fee | **Passed** | A third mint (tx `eZbGgUGhefnjHksFxQK8Yn4jq2CZ5Zc1r6CYpK7FAz68uDAZZCyw962ydF3uYvq57TGhz1QdtgqYLFNmk17xRDw`) changed the Reserve Token supply partway through the accrual period; `periodSupplySeconds` grew from `267,300,000` (after the first checkpoint window) to `701,300,000` (after the second, larger-supply window) -- the accumulator correctly integrated BOTH supply levels over their respective real elapsed durations, not one flat end-of-period snapshot. |
| 12 | Weekly settlement cannot double-charge (retry-safety) | **Passed** | An immediate second `accrue_fees` call after settlement (tx `47iCxa7GGoEbLHpk46zTL23SVvmJ6x4d3nnnsU87ACMVztguSPAv82NYeHH4w3tYu56CNtWRpaE656N88nt86Huo`) billed ONLY the new ~2-second sliver of real elapsed time since the first settlement (independently recomputed expected value matched exactly) -- the already-settled 701,300,000 supply-seconds was never re-included. `TvlAccrual.last_settled_ts` only ever advances forward, `period_supply_seconds` only resets on an actual successful settlement. |
| 13 | Failed keeper runs can retry safely | **Passed (proxy)** | Same mechanism as item 12: `accrue_fees` is idempotent-safe to call repeatedly at any cadence -- a failed/unconfirmed prior attempt never advances `last_settled_ts`, so a retry bills the true elapsed window from the last SUCCESSFUL settlement, never double-counting a prior attempt's (never-committed) work. Not separately exercised via an intentionally-failed transaction this pass (would require deliberately triggering a mid-instruction abort, which the on-chain design makes structurally hard to induce from a client). |
| 14 | Protocol weekly shares reach treasury automatically | **Passed** | Both `accrue_fees` calls above minted the Protocol's settled share directly to the treasury ATA within the SAME `accrue_fees` transaction (treasury balance deltas of +2 and +1 respectively, matching the independently-computed expected values exactly) -- no separate transfer step, no manual trigger. |
| 15 | Manager weekly shares accrue to recipients | **Passed** | The Manager recipient's `pendingFeeShares` increased by exactly its apportioned share (+1 unit) in the same settlement transaction; recipientB's share rounded to 0 at this tiny scale, consistent with the largest-remainder apportionment (`apportionToRecipients`) -- confirmed by the same independent recomputation. |
| 16 | Protocol Fees section and manual "Send to Treasury Now" button are absent from Manage Reserve | **Passed (code review)** | The entire "Protocol Fees" `<div>` block (pending-balance display + `executeCollectProtocolFee` button) was deleted from `ManageDTR.tsx`; the now-unused `executeCollectProtocolFee` import was removed. Confirmed by source read and a clean `tsc -b --force`/`oxlint`/`npm run build` (no dangling references). Not interactively screenshotted in a browser this pass. |
| 17 | Activity records contain correct amounts/wallets/signatures | **Passed (decoder-level, offline) + structural (indexer)** | `packages/sdk/src/activityLog.ts`'s `summarizeActivityEvent` now decodes `reserveTokensMinted`, `reserveTokensRedeemed`, `protocolMintFeeTransferred`, `tvlFeeSettled`, `managerFeeShareAccrued`, `managerFeeShareCollected`, `managerFeeRecipientsConfigured`, `reserveCreated`, and `reserveAssetInitialized` -- each unit-tested against real field shapes (`tests/phase_road_to_mainnet_feedback.ts`). The Postgres indexer (`lib/reserve-activity/indexer.ts`) is fully generic (walks every emitted event via this same decoder), so it will pick these up automatically once the new Reserve's activity is synced -- not separately confirmed by reading rows back out of Postgres for reserveId 65 this pass (the indexer syncs on-demand per `GET /api/devnet/reserve-activity`, not proactively). |

## Additional coverage beyond the user's explicit list
- **Weekly keeper (`api/devnet/accrue-fees-cron.ts`) rewrite**: `?dryRun=true` invoked locally against the live deployed program via `scripts/verify_keeper_local_2026_08_14.ts` -- successfully discovered 20+ real overdue Reserves across the whole protocol (8-17+ days since last accrual each), confirming the discovery/reporting path works end-to-end post-upgrade. The keeper's actual settlement transaction shape (`accrueFees()` with `protocolConfig`/`mintAuthority`/`tvlAccrual`/`protocolFeeDestinationTokenAccount`/`protocolFeeDestination`/`managerFeeRecipients`/`payer` accounts) is IDENTICAL to the one directly exercised and proven correct in items 10-15 above -- see "Known gaps" for why the full unconditional-loop-over-every-Reserve real run wasn't completed live this pass.
- **IDL structural self-verification**: `tests/phase_protocol_fee_auto_deposit.ts` gained 8 new assertions (23 total in that describe block) checking every hand-patched IDL account list/discriminator introduced this pass against the actual Rust struct definitions -- all passing.

## Preconditions (not fee-distribution evidence on their own, but required before the above was attempted)
- 594 offline tests passing (`npx ts-mocha -p ./tests/tsconfig.json -t 60000 <all tests/*.ts except ssr_protocol.ts>`).
- `tsc -b --force`, `npx tsc -p api/devnet/tsconfig.json --noEmit`, `oxlint`, full `npm run build` (SDK workspace + app) all clean.
- `cargo check -p ssr_protocol` and `cargo-build-sbf --manifest-path programs/ssr_protocol/Cargo.toml` both clean, producing a real deployable `target/deploy/ssr_protocol.so` (874,952 bytes).

## Known gaps / not independently re-verified this pass
- **RPC provider**: `HELIUS_RPC_URL` in this session's `.env.local` was a redacted `[SENSITIVE]` placeholder, not a usable key -- every live transaction and read in this pass went through the public DevNet RPC instead, which is confirmed (repeatedly, across this project's history) to rate-limit under heavy sequential use. The direct verification script (items 1-15) completed successfully despite this; the keeper's FULL real run across every one of the protocol's 20+ overdue Reserves was not completed live in this session because iterating that many Reserves' worth of RPC calls against the public endpoint in one sitting is impractical -- only its `dryRun` discovery path and (via the separate direct script) its exact per-Reserve transaction shape were exercised. The keeper's first live Vercel Cron trigger, running against the production Helius key, will be this endpoint's first genuinely complete real-world run across every Reserve.
- **Frontend UI, interactively**: the Manager Fee Recipients per-row independent Collect buttons, the "Claimable by this wallet" read-only badge for a non-connected recipient, and the Activity Log's new mint/redeem/instant-transfer/TVL-settlement entries were all verified by source/type-check review (clean `tsc -b --force`, no other call sites broken) but not clicked through in a running browser this pass -- no local dev server session was part of this verification.
- **Deliberately-failed keeper retry**: item 13's retry-safety was proven via two back-to-back SUCCESSFUL `accrue_fees` calls (proxy for "a failed run followed by a retry"), not a genuinely aborted/reverted transaction followed by a real retry -- the underlying mechanism (idempotent settlement gated purely by `last_settled_ts`) makes the two cases structurally identical, but this was not separately staged as a deliberate on-chain failure.
- **Activity Log rows for this pass's own test Reserve**: not read back from Postgres for reserveId 65 specifically (the indexer syncs on first `GET` request for a Reserve, which wasn't issued against this exact Reserve this pass) -- the decoder itself (which is what actually determines correctness) is unit-tested directly instead.
