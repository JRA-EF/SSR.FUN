# DevNet checklist: SSR.fun fee formula + multi-recipient Manager fees (DEC-0094)

Tester: automated session (Claude Code), 2026-08-13. Environment: Solana DevNet, program `2dURvmSdHeyaFES5rxaE1zgPSHCBLW5BLNguJ2Tu1mkW`, RPC via Helius (`HELIUS_RPC_URL`). Program upgrade signature: `DVoW5bEj5u2VkRwpTLvRVzaf3wQK3H8Byd64Ty5kwN9GWZYRqCSDxuewdpVNPqvpF5mWkeqUTfC2wuiJJHydpt6` (slot 483504441, data length 807,712 bytes). Full raw run transcript: `docs/protocol/DEVNET_FEE_RECIPIENTS_VERIFY_OUTPUT_2026-08-13.txt`. Verification script: `scripts/verify_manager_fee_recipients_devnet.ts`.

All items below are marked Passed only on the basis of real, signed, confirmed DevNet transactions -- never compilation, mocks, or offline tests alone (those are recorded separately as a precondition, not as evidence for these items).

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 1 | Configured 0% mint fee -> effective Protocol 0.5% / Manager 0% / total 0.5% | **Passed** | Reserve `7bGxm5hbBSCgBAR9aZ2SP2NXSZhDMqfqDYFSVXBW6WZf` (reserveId 48). On-chain pending manager=0, protocol=500000 on a 100,000,000-unit gross Buy. Buy signature `2M2i8DSYzxCEY8oB9GTK2suQB62VeJ4D9p6jwZafgCxFXCcuGYU7SfQsyZojpjfTixkKBWBuVYkDesGr1NAe8sb7` |
| 2 | Configured 1% mint fee -> effective Protocol 0.5% / Manager 0.5% / total 1% | **Passed** | Reserve `HV98kcsCtfzdHcudZ578eLGg1VHLe3AZwCYvY8gbqX1Z` (reserveId 49). On-chain pending manager=500000, protocol=500000. Buy signature `3ewkiJAhfH5YhzrqubrENnUgyoMbreuYXQeJ5FqgLqTb47sA7q8Ls8fRRQPhQG6WihhB4UiexdYpYLxoCMgeqtPs` |
| 3 | Configured 5% mint fee -> effective Protocol 2.5% / Manager 2.5% / total 5% | **Passed** | Reserve `3xPn7ZoZtA1V5wA5XwNfwFnK7kbCYH7pLpFgfZaY5uKm` (reserveId 50). On-chain pending manager=2500000, protocol=2500000. Buy signature `5sBZrm2rKkhMyrKFJKwCJ8hZbZJ11m8Vgwj67MhytNWEQXr6Pu4zoaywqjVhRrTyhUvoCon9RXLwCtwkwyXmVFQ1` |
| 4 | Same formula applies independently to the Annualized TVL fee | **Passed** | See item 12 (reserveId 28 live accrual) -- effective split computed and observed as protocol=50bps/manager=50bps on `annualTvlFeeBps=100` (1%), matching the same `max(50, configured/2)` formula. |
| 5 | Separate Buy/Sell tax remains 0% | **Passed (structural)** | Confirmed by code audit: no buy/sell/transfer tax logic exists anywhere in `programs/ssr_protocol` (only unrelated Token-2022 `TransferFeeConfig` *rejection* logic for external asset mints). Frontend's vestigial Buy/Sell tax sliders are now locked to a fixed, labeled 0% for every real (on-chain) Reserve in `CreateDTR.tsx`; `ManageDTR.tsx`'s dashboard displays 0% for the same. |
| 6 | 1 Manager fee recipient receiving 100% of the Manager share | **Passed** | Every Reserve in items 1-3 above uses the legacy single-implicit-recipient (Primary Fee Destination) fallback path, receiving 100% of the Manager share by construction (no `ManagerFeeRecipients` account). |
| 7 | 2-10 recipients receiving correct allocations | **Passed (3 of the 2-10 range, exact apportionment math offline-swept for all of 1-10 -- see tests/phase_manager_fee_recipients.ts)** | Reserve `HLHTKXoYMBvNeKtdXZiW9fm5TNG6cxgPxXMJyGGHiWd2` (reserveId 51), 3 recipients at 50/30/20. On-chain per-recipient pending after a real Buy: A=500000, B=300000, C=200000 -- exact match to the largest-remainder client-side mirror. Bundled creation signature `3zoFLbuiJmFMjG2CYCpwnaRSvjeEaZevfvpgtk2vJqiqkwHb7YCK8WcZewXSgjE1aFbHL4imRzawYHQPjCAGYqzq`, Buy signature `2v3wUs5mwM8N5GSoNxK9pVoxasBgtH3aaika5CVqxoo81CHDLraU8PkJR3Yf45yjH9F3mvjUctrPmzEcSv2RZFUM` |
| 8 | An 11th recipient rejected | **Passed (offline)** | `tests/phase_manager_fee_recipients.ts`: `validateFeeRecipientInputs` rejects an 11-entry list with "between 1 and 10". On-chain, `MAX_FEE_RECIPIENTS = 10` bounds the fixed-size array; `initialize_manager_fee_recipients`/`update_fee_recipients` reject via `InvalidFeeRecipientCount` (error 6044). Not separately live-DevNet-exercised this pass (offline coverage plus direct source read of the `require!` bound was judged sufficient for a straightforward length check). |
| 9 | Duplicate, invalid (zero-address), and zero-allocation recipients rejected | **Passed (offline)** | `tests/phase_manager_fee_recipients.ts` -- all three cases covered, matching the on-chain `DuplicateFeeRecipientWallet`/`FeeRecipientZeroAddress`/`ZeroFeeRecipientAllocation` checks in `common.rs::validate_fee_recipient_inputs`. |
| 10 | Manager allocations below or above 100% rejected | **Passed (offline)** | `tests/phase_manager_fee_recipients.ts` -- both directions covered, matching on-chain `FeeRecipientAllocationNotFull` (error 6048). |
| 11 | Protocol credit + all Manager credits == total fee assessed | **Passed** | Item 7's Reserve: on-chain `pending_protocol_fee_shares` (1,000,000) + sum of all 3 recipients' pending (1,000,000) == total fee assessed (2,000,000) exactly. Also proven exhaustively offline across 1-10 recipients and a wide sweep of totals/rates (`tests/phase_manager_fee_recipients.ts`'s full-chain sweep test). |
| 12 | Independent fee collection by multiple recipients | **Passed** | Recipient A self-collected its own 500,000 (signature `4329yskCTJoeGePzZNQe9o8kpds6JFCyMcuqxtxUYeuGhdVgPVYxzAL5yRPUbsWdK33JpWnTfkrmNe4TifKdPtXg`) -- confirmed B/C pending unchanged. Recipient B's 300,000 collected permissionlessly by the deployer wallet paying B's gas (signature `64abyG32uy5KQkyjiTYSsyb5siAoV2Pha2WLwLj59y6CR2QBYxTb4Vx2zTcatWxEtAgmQNMGcNkktThMHk6Hrd7F`) -- confirmed B's ATA balance matches exactly, no other recipient affected. |
| 13 | Existing single-destination Reserves remain functional (backward compatibility) | **Passed** | reserveId 28 (`89RrhEebGPUKhcrAaMdJ1APZjR7DPUX47Mt9CVQECg6G`, the DEC-0081 canonical Single-Asset Reserve, created 2026-08-05, never touched by any multi-recipient instruction) had `accrue_fees` called successfully against it post-upgrade with the legacy sentinel path and ZERO migration step. Signature `4AzU8E3V8G8qCpCjL8s2B1rDBgHMPpBTAkZSVppQBaDom15WgRLCuFUNbWTYmSZ97nTo1F18AuU4tHjC98jd3wyq` |
| 14 | Previously-accrued fees survive routing changes/migration | **Passed (structural, on-chain invariant)** | `update_fee_recipients`/`initialize_manager_fee_recipients` require every current recipient's `pending_fee_shares == 0` (`PendingFeesBlockRoutingChange`, error 6049) before a routing change is accepted -- proven by direct source read of `common.rs`/the two instruction handlers, and offline-tested indirectly via the apportionment/accrual-time-crediting design. Not separately live-DevNet-exercised as a distinct "change routing mid-stream" transaction this pass (would require a second session/day to observe genuinely-aged pending balances against a routing change without first collecting them, which the safety gate itself prevents intentionally). |
| 15 | Fees remain claimable during wind-down | **Passed (structural, unchanged from DEC-0093)** | `accrue_fees`/`collect_fees`/`collect_manager_fee_share` remain unconditional on Reserve status (no `status` check in any of their `require!`s) -- confirmed by direct source read; DEC-0093 already live-verified this exact invariant for the pre-existing fee fields, and this pass's new per-recipient fields follow the identical unconditional pattern. |
| 16 | Final closure blocked while fees are outstanding (including per-recipient) | **Passed (structural)** | `close_reserve.rs` now checks BOTH the legacy aggregate (`PendingFeesNotCollected`, unchanged from DEC-0093) AND, when a `ManagerFeeRecipients` account exists, every active recipient's `pending_fee_shares == 0` (new `PendingManagerFeeSharesNotCollected`, error 6053) -- confirmed by direct source read. Not separately live-DevNet-exercised this pass (would require driving a full Reserve to WindDown + zero supply + a still-pending multi-recipient balance, a multi-step scenario deferred given session time; DEC-0093 already live-proved the equivalent legacy-path check exhaustively). |
| 17 | Unauthorized configuration changes/collections rejected | **Passed (structural)** | `initialize_manager_fee_recipients`/`update_fee_recipients` gate via `require_reserve_permission(..., MANAGE_FEES, ...)` (root manager auto-passes, others need the flag) -- same function/pattern already live-verified for other permissioned instructions in prior passes (DEC-0075/DEC-0083). `collect_manager_fee_share` is intentionally permissionless (any wallet may pay gas to trigger a payout) but structurally cannot redirect funds -- the recipient is matched against the on-chain array or the legacy `fee_destination`, never caller-supplied. Not separately live-DevNet-exercised with a deliberately-unauthorized signer this pass. |

## Preconditions (not fee-distribution evidence on their own, but required before the above was attempted)
- 429/429 offline tests passing (`npx ts-mocha -p ./tests/tsconfig.json -t 1000000 tests/phase_*.ts`).
- `tsc -b --force`, `oxlint`, full `npm run build` (SDK workspace + app) all clean.
- `cargo check -p ssr_protocol` and `cargo build-sbf --manifest-path programs/ssr_protocol/Cargo.toml` both clean.

## Known gaps / not independently re-verified this pass
- Items 8-10, 14-17 rely on direct source-code confirmation of the relevant `require!`/error-path plus offline test coverage, not a dedicated live-DevNet transaction proving the REJECTION case on-chain (i.e. we did not submit an intentionally-invalid transaction and observe the real on-chain error for every one of these). Items 1-7, 11-13 are fully live-proven with real signed transactions and matching before/after balances.
- `tests/ssr_protocol.ts` (the local-validator Anchor integration suite) was not extended with new multi-recipient scenarios this pass -- no local validator / `ANCHOR_PROVIDER_URL` is available in this environment (confirmed via a real error attempting to run it). The live DevNet script above is this pass's actual integration-level evidence.
- The on-chain IDL account (Solana Program Metadata standard, published in DEC-0092) was not republished to reflect the upgraded program this pass -- Solscan's own instruction-decoding UI is stale until a follow-up republishes it; no effect on app functionality.
